story: US-3.3

current_stage: SECURITY_REVIEW

status: IN_PROGRESS
# VERIFICATION done 2026-09-02: docs/verification/US-013-verification-
# report.md - Pass. No ORM leak, no missing eager-load (N/A - no
# relationships), no TTL-less cache write (N/A - no cache.py in this
# module), no service->router cross-module call, no missing
# response_model/status_code. Both migrations' downgrade() perform real
# inverse DDL. All 5 SS5 security cases exist by name for the one
# protected route; AU-AC4's 405 immutability additionally runtime-
# verified (test_patch/put/delete_audit_logs_returns_405), not just the
# PLANNING-stage handler-reasoning check. advisor caught 2 gaps in the
# first draft, both fixed same-day: an ambiguous git diff invocation
# backing the .env.example N/A claim (re-verified with git branch
# --list main + a standalone git diff --stat, confirmed empty/exit 0);
# the SS6.5 "generated file read" row wrongly marked N/A instead of
# Pass (the migrations were read, just hand-written not autogenerated).
# Advancing to SECURITY_REVIEW.
#
# IMPLEMENTATION done 2026-09-02 (T1-T9 complete). gate-enforcer PASS:
# 6/7 pre-commit hooks clean (ruff lint+format, mypy strict on 131 files,
# import-linter 6/6, unit tests, no-mock-in-integration guard); 7th
# (detect-secrets) blocked by this environment's WDAC policy (WinError
# 4551), independently confirmed via direct hook invocation as not a
# real secret finding - committed with --no-verify under the user's
# standing per-session authorization (see project_precommit_detect_
# secrets_wdac.md auto-memory). 556/556 tests, 96.39% coverage (floor
# 85%; audit/service.py and audit/router.py both 100%, floor 90%).
# lint-imports 6/6 contracts kept. T7/T7b/T8/T8b (61 audit-scoped tests)
# hardened after advisor review before this gate run: the genesis-hash
# test now asserts its own empty-table precondition explicitly instead
# of relying on test-ordering/rollback isolation; the pagination query-
# plan test now asserts the real EXPLAIN signal (Index Scan feeding
# Merge Append, no Seq Scan) instead of a substring check that couldn't
# fail; AU-AC6's CI-grep docstring narrowed to state plainly it's a
# field-name check, not a runtime-value scan. Committed 96ec17a (T8/T8b/
# AU-AC6-scan/schema-tests) + f231937 (ruff-format autofix). Advancing
# to VERIFICATION.
#
# IMPLEMENTATION T1/T2/T2b/T3/T3b/T3c done 2026-09-02, committed 15fcc3b
# on feat/us-3.3-view-audit-information (branched off main post-US-3.1
# merge). New app/modules/audit/ (schemas/models/repository);
# email_verification rename (OD-1); audit_log table + hash-chain
# trigger + audit_log_history view + 4 new indexes (3 migrations,
# upgrade/downgrade/upgrade proven against real Postgres 16). Two real
# defects caught by advisor and fixed pre-verification: OD-6 concurrent-
# insert race (SELECT...FOR UPDATE doesn't serialize under MVCC when the
# locked row isn't modified - replaced with pg_advisory_xact_lock,
# proven against genuinely overlapping transactions) and a missing
# covering index on audit_log itself (trigger's seed query would have
# full-scanned every insert). migrations/env.py touched with explicit
# user sign-off. See docs/catalog/US-3.3-pipeline-status.md for full
# detail. Next: T4/T5 (service-and-router-builder).
#
# TESTS done 2026-09-02: docs/tests/US-013-traceability-matrix.md (matrix
# only, test code deferred to IMPLEMENTATION T7/T7b/T8 per the T1-T9
# breakdown). All 9 ACs + cross-cutting AGENTS.md SS5 security cases
# mapped to planned test function names. advisor caught 2 real gaps in
# the first draft, both resolved same-day: (1) AU-AC6's original test
# row would have passed trivially (this story's own 2 event types carry
# none of the 5 named secret classes) - added the story's own actual
# AU-AC6 mechanism, a CI grep over audit-write call sites, as a second
# row, and reframed the integration test as a regression guard, not
# AU-AC6's primary proof; (2) AU-AC9's retention job had no file, task,
# or test anywhere in the plan/task-breakdown despite being a named AC -
# same class of gap as T6/scripts/verify_audit_chain.py at PLANNING.
# Also surfaced: the hash-chain genesis rule (FR-7) was still an
# unresolved carried-forward Open Question blocking T3b's trigger SQL,
# which blocks T4/T8/T9 downstream - the same shape as OD-14/15/16 and
# should not have been left as a footnote. Both raised to the user:
# OD-17 (genesis rule) resolved - fixed sentinel for the very first
# partition, subsequent days seed from the most recent non-empty prior
# partition's final row_hash (skipping empty days); OD-18 (AU-AC9 scope)
# resolved - explicitly deferred out of this story's build scope,
# blocked on OD-9's still-open legal/DPO cold-storage decision, no code/
# task/test delivered against it. docs/decisions/US-3.3-open-decisions.md,
# docs/designs/database/US-013-db-design.md, docs/plans/US-013-
# {implementation-plan,task-breakdown}.md, and the traceability matrix
# all updated to match. Advancing to IMPLEMENTATION per T1-T9.
#
# PLANNING fully closed 2026-09-02: OD-15 (script ownership) resolved -
# service-and-router-builder builds scripts/verify_audit_chain.py (T6).
# OD-16 (partition safety net) resolved - T3b's migration also creates
# a DEFAULT partition, preventing the hard-INSERT-failure risk planner
# flagged. docs/plans/US-013-{implementation-plan,task-breakdown}.md and
# docs/designs/database/US-013-db-design.md updated to match. Advancing
# to TESTS.
#
# PLANNING (plan-reviewer) done 2026-09-02: docs/reviews/plans/US-013-
# plan-review.md - Pass with Issues. Full impact-analysis coverage, no
# layering-order violations, realistic risk/test strategy. 1 process
# finding (non-blocking, fixed same-day): scripts/verify_audit_chain.py
# was never listed in impact-analysis.md despite being derivable from
# the spec at that stage - impact-analysis.md amended in place to add
# it. 2 genuinely open decisions correctly disclosed rather than
# guessed, both need the user before TESTS/IMPLEMENTATION can start:
# T6's script ownership (no execution skill in this project's roster
# covers scripts/) and planner's partition-maintenance mechanism choice
# (DEFAULT partition vs. a maintenance-job task added to this story).
# Awaiting user resolution.
#
# PLANNING (implementation-planner) done 2026-09-02: docs/plans/US-013-
# task-breakdown.md. T1-T9 (+T2b/T3b/T3c/T7b). advisor caught 2 real
# dependency-graph defects, both fixed: (1) T7 originally depended on
# T6, which has no assigned owner - a test task depending on an unowned
# task breaks the whole sequence at T9; split into T7 (audit unit tests,
# depends T4) and T7b (verifier unit test, depends T6, explicitly noted
# as blocked pending T6's ownership decision); (2) T8 (integration
# tests updating the renamed email_verification tests) was missing T2b/
# T3 in its dependency list despite needing the rename actually applied
# to test against - added. Also added the sa.inspect(op.get_bind())
# guard note to T3c (CONCURRENTLY indexes are hand-written, outside the
# Rewriter's reach, per AGENTS.md SS4). T6's ownership (no execution
# skill in this project covers scripts/) and planner's partition-
# maintenance decision are flagged as one bundled open question for the
# user, not invented here. Advancing to plan-reviewer.
#
# PLANNING (planner) done 2026-09-02: docs/plans/US-013-implementation-
# plan.md. New app/modules/audit/ module; audit_log table + trigger +
# audit_log_history view; OD-1 rename migration must precede audit_log
# creation; 4 CONCURRENTLY indexes; FR-3 denial-write resolved as an
# endpoint-local require_audit_read wrapper (not a shared require_scope
# change); migrations/env.py §7.9-protected touch flagged with precedent
# citation (US-3.1/US-3.2). advisor caught 3 gaps, all fixed: (1)
# AU-AC7's [gate] test as originally described needed a "privileged
# connection" that OD-12 already established doesn't exist (app is
# superuser) - reworded to mutate-via-ordinary-session; (2) the chain-
# verification job itself (scripts/verify_audit_chain.py) was completely
# missing from Files To Create despite being [gate]-marked and called by
# the Testing Strategy - added, plus its unit test; (3) partition-
# maintenance risk sharpened from generic "hard failure eventually" to
# precise mechanism (no DEFAULT partition -> first post-window INSERT,
# including this story's own FR-2 write, 500s immediately) with 2
# concrete resolution options for plan-reviewer to pick between rather
# than leaving it open. Advancing to plan-reviewer.
#
# PLANNING (impact-analysis) done 2026-09-02: docs/impact-analysis/
# US-013-impact-analysis.md. New app/modules/audit/ module (7 files).
# Rename app/modules/email_verification/models.py::AuditLog table (OD-1).
# 4 new occurred_at indexes on existing audit tables (CONCURRENTLY).
# advisor caught 2 issues, both fixed: (1) verified the implicit-405
# claim against app/main.py's actual registered exception handlers
# (none intercept it) instead of leaving it as an open question for
# implementation; (2) db-design/entity-model still described the 4
# existing tables as "frozen"/"historical" - stale language from the
# earlier (superseded) broad-cutover draft; corrected throughout both
# files to reflect staged OD-14 (they stay live, ongoing writes,
# primary query surface for audit_log_history, not a compatibility
# shim). Real architectural tension flagged, not resolved: FR-3's
# denial-write is not free (require_scope writes nothing on 403 today)
# - narrow endpoint-specific wrapper vs. widening require_scope itself,
# left for planner. Advancing to planner.
#
# OD-14 resolved 2026-09-02 (user): staged (option 3). audit_log + hash
# chain + this story's own 2 new events ship now; repointing the 4
# existing modules' audit writes is a future follow-up, one module at a
# time. docs/designs/database/US-013-db-design.md and entity-model.md
# updated to reflect this as confirmed, not a working assumption.
# Advancing to PLANNING.
#
# DESIGN done 2026-09-02: docs/designs/api/US-013-{openapi.yaml,api-
# design.md} (1 GET endpoint + 405 on PATCH/PUT/DELETE; limit/cursor
# reuse admin_users precedent per OD-5; per-entry {id} 405 path flagged
# as unmodeled routing question). docs/designs/database/US-013-{db-
# design,entity-model}.md - central finding: audit_log MUST be a real,
# daily-partitioned physical table, not a pure view (a view can't be
# partitioned, and AU-AC7's BEFORE INSERT trigger needs a physical
# table) - this is a technical conclusion, not a judgment call, and
# resolves the spec's FR-2/FR-3 write-target Open Question and moots the
# earlier assumption that the 4 existing per-domain tables need new
# columns. advisor caught 2 real issues in the first draft, both fixed:
# (1) the design had silently decided to repoint all 4 existing modules'
# audit-write call sites to the new table - genuinely undecided scope,
# not a technical requirement (AU-AC7 is satisfied either way); logged
# as OD-14 (High) with 3 priced options, design now proceeds on the
# staged/narrowest option as a non-binding working assumption pending
# the user's answer; (2) db-design claimed the 4 existing tables'
# current indexes already support audit_log_history's occurred_at-DESC
# keyset pagination - false, verified against the live models (none
# indexes occurred_at) - fixed by adding one new index per table,
# needed regardless of OD-14's outcome. Awaiting user resolution of
# OD-14 before PLANNING (impact-analyzer needs to know the real blast
# radius to survey).
#
# SPEC_REVIEW third run done 2026-09-02: docs/reviews/specifications/
# US-013-spec-review.md - Pass with Issues, clean this time, no override
# needed. 9/9 ACs Covered, zero Contradictions - both prior Fail triggers
# resolved by amending the story itself rather than accepting a
# disclosed gap. 6 non-blocking ambiguities/edge-case findings remain
# (mostly carried forward from the pre-existing 2026-08-22 review),
# flagged for DESIGN to address, most notably the audit_log table-vs-
# view write-target question (FR-2/FR-3) and the hash-chain genesis rule
# (FR-7). Advancing to DESIGN cleanly - openapi-designer/db-designer's
# own "never Fail" precondition is now satisfied without needing the
# orchestrator override recorded in the (now superseded) SPEC_REVIEW
# re-run entry below.
#
# STORY AMENDED 2026-09-02: user chose to amend the scope source of
# truth rather than override openapi-designer/db-designer's own stated
# "never Fail" precondition. docs/stories/US-3.3-view-audit-information.md
# edited: AU-AC4 split into this story's API-level 405-only scope plus a
# documented note tracking DB-grant enforcement as a separate, project-
# wide infrastructure follow-up (OD-12); Data Model Notes' "monthly"
# partition line corrected to "daily" (OD-11), removing the story's own
# self-contradiction. No other AC changed. docs/specifications/US-013-
# view-audit-information-spec.md updated to match (AU-AC4/FR-4 now
# honestly fully Covered, no divergence to disclose). Re-running
# story-spec-reviewer once more to confirm an honest Pass before DESIGN.
#
# SPEC_REVIEW re-run done 2026-09-02: docs/reviews/specifications/
# US-013-spec-review.md re-run after OD-13 (field-aware redaction for
# AU-AC8, raised by the first SPEC_REVIEW run and resolved same-day) -
# verdict still Fail, mechanically unchanged, because AU-AC4's Partial
# Coverage and the daily/monthly Contradiction cannot resolve to Pass
# without amending docs/stories/US-3.3-view-audit-information.md itself,
# which the user explicitly chose not to do (selected "Accept and
# advance" over "Amend the story text" when asked). The AU-AC8 High
# finding from the first run is fully resolved and no longer appears.
# EXPLICIT ORCHESTRATOR OVERRIDE, recorded per this project's "report
# conflicts, don't silently relax a rule" norm: story-orchestrator's
# default rule is to stop on any Fail. Here the user was shown the
# review's findings via AskUserQuestion and explicitly chose to accept
# AU-AC4/the partition-contradiction as known, already-decided AC-
# coverage gaps (matching OD-12/OD-11's own resolutions) rather than
# amend the story - an informed, explicit risk acceptance, not a silent
# skip. Advancing to DESIGN on that basis. Both gaps are disclosed in
# docs/specifications/US-013-view-audit-information-spec.md's Open
# Decision Resolutions section and Out of Scope; anyone reading the spec
# sees them without needing this pipeline history.
#
# SPEC_REVIEW done 2026-09-02: docs/reviews/specifications/US-013-spec-
# review.md - Fail. 8/9 ACs Covered on independent literal-text
# comparison. AU-AC4 Partially Covered (its Gherkin has a 405 clause AND
# a DB-grant clause; FR-4 delivers only the 405 - the DB-grant half was
# already deferred via OD-12). 1 Contradiction: spec's Data Model Notes
# say daily partitions, source's own Data Model Notes line says monthly
# (source was self-contradictory; OD-11 already resolved "daily" as
# authoritative). Both Fail triggers are bookkeeping - already-resolved,
# user-approved ODs recorded at the correct pipeline stage, not new
# defects. One genuine NEW finding independent of the Fail mechanics:
# [High] no mechanism stated for locating/redacting a user's identifiers
# embedded in structurally inconsistent fields across the 4 unioned
# tables for AU-AC8/FR-8 - concretely, profile_audit_log.old_value/
# new_value for a field="display_name" row literally contains the
# changed display_name (one of AU-AC8's 3 named identifiers), and
# nothing states how the erasure script finds it. advisor confirmed the
# Fail verdict and flagged this as the item actually needing a decision
# (the other two are already decided). Awaiting user disposition: (a)
# accept AU-AC4/monthly-contradiction as known/approved AC-coverage gaps
# and advance to DESIGN, plus decide how to handle the new AU-AC8
# redaction-mechanism gap; or (b) amend docs/stories/US-3.3-view-audit-
# information.md's AU-AC4 text to move the DB-grant clause to the
# follow-up story (makes AU-AC4 honestly Covered, spec review Pass) -
# user's call since it edits the scope source of truth.
#
# SPECIFICATION done 2026-09-02: docs/specifications/US-013-view-audit-
# information-spec.md revised in place (pre-existing 2026-08-22 draft).
# Incorporates OD-1/OD-2/OD-11/OD-12 resolutions as an "Open Decision
# Resolutions" section; adds a Data Model Notes section and the RFC 7807
# error envelope, both previously omitted (flagged by the pre-existing
# spec review). advisor cross-check caught 3 real gaps the first draft
# glossed over, fixed same-day: (1) AU-AC1/FR-1's actor_role/outcome/
# category fields aren't stored on any of the 4 existing per-domain
# tables - noted in FR-1 and the traceability matrix (matching AU-AC4's
# honest annotation) rather than silently assumed; (2) FR-2/FR-3's own
# audit-write target is undecided while OD-1 only settled audit_log's
# *name*, not whether it's a writable table or a read-only view - logged
# as a new Open Question; (3) FR-7's hash chain had no genesis rule for
# the first partition or an empty-day gap - logged as a new Open
# Question. 9/9 source ACs traced. docs/stories/README.md's backlog
# table left unchanged (git history confirms it was never updated by
# any prior story - single-commit precedent). Advancing to SPEC_REVIEW.
#
# CLARIFICATION done 2026-09-02, then resolved same day: user accepted
# the recommended option on all 4 blocking Open Decisions - OD-1 rename
# email_verification.AuditLog/audit_log to unverified_account_purge_log
# (frees `audit_log` for this story); OD-2 build a minimal provisional
# DA-AC9 script (anonymize users row + redact identifiers on that user's
# audit rows), legal/DPO anonymize-vs-delete question stays open
# separately; OD-11 daily partitions (Data Model Notes' "monthly" line
# superseded); OD-12 ship only the API-level 405 for this story, DB-role/
# GRANT-REVOKE infrastructure deferred to a separate project-wide follow-
# up. See docs/decisions/US-3.3-open-decisions.md (Resolved notes under
# OD-1/OD-2/OD-11/OD-12) and docs/evidence/US-3.3-clarification-report.md
# (verdict now Ready for Specification). Advancing to SPECIFICATION.
#
# CLARIFICATION started 2026-09-02: us-clarifier verdict Not Ready. 12 Open
# Decisions logged (docs/decisions/US-3.3-open-decisions.md), 4 BLOCKING:
# OD-1 the story's own Assumptions table and Data Model Notes disagree on
# whether `audit_log` is a new central table or a read-only view, and
# either reading collides with a real, already-shipped, unrelated table
# of that name (app/modules/email_verification/models.py::AuditLog,
# BR-002's purge job); OD-2 AU-AC8 cites "the US-1.4 DA-AC9 retention
# job" as already built - it isn't (DA-AC9 is [manual]/deferred pending
# DPO sign-off in its own story and BR-007); OD-11 the story contradicts
# itself on partition granularity (Assumption #4 + AU-AC7 say daily,
# Data Model Notes says monthly); OD-12 AU-AC4's [gate]-tested DB-grant
# immutability requirement has no infrastructure behind it anywhere in
# this codebase (zero GRANT/REVOKE in any migration) and the app's
# configured DB connection is the Postgres superuser role, which cannot
# be restricted by grants at all. OD-3/OD-4 are Medium scope-sizing notes
# (existing audit tables missing required columns; ticket_audit_log
# doesn't exist since Epic 4 is unbuilt), non-blocking, recommendations
# stated. OD-5 (limit/cursor bounds) resolved by admin_users precedent.
# OD-6-OD-10 carried forward unresolved from the pre-existing 2026-08-22
# draft spec/review (docs/specifications/US-013-view-audit-information-
# spec.md, docs/reviews/specifications/US-013-spec-review.md), which
# predate US-3.1/US-3.2 landing on main and could not check the story
# against the real codebase or against itself as closely as this run.
# audit:read permission dependency (US-3.2) independently confirmed
# live and satisfied. See docs/evidence/US-3.3-clarification-report.md.
# Awaiting user resolution of OD-1/OD-2/OD-11/OD-12 before advancing to
# SPECIFICATION.

# --- prior story: US-3.1 (DONE, reached PR, merged) ---
# story: US-3.1
# current_stage: PR
# status: DONE
# PR done 2026-09-02: docs/pr/US-011-pr-description.md drafted by
# pr-preparer. All 4 gates confirmed Pass from their actual reports
# (gate-enforcer 7/7 + 509/509 tests/96.64% coverage; implementation-
# verifier Pass; security-reviewer Pass; reconciliation-reviewer Pass).
# .env.example alignment confirmed (2 new settings, 1:1 with config.py).
# Commit hygiene confirmed via git status/git log - 6 commits, every
# changed file traces to this story's scope. User explicitly authorized
# push+PR ("go"). Pushed to origin, opened as GitHub PR #12
# (https://github.com/AndriiDobrovolskii/Customer_Portal/pull/12).
# RECONCILIATION done 2026-09-02: docs/reconciliation/US-011-
# reconciliation-report.md - Pass (initial pass Fail, 2 gaps found+
# fixed same-day). All 21 source ACs + FR-17b/FR-22/FR-23 (no-AC) have
# full, verified matrix coverage. Diffing every matrix-cited test
# function name against the real test files found 31 mismatches - 29
# were harmless pre-implementation-to-shipped renames (individually
# verified), 2 were genuine: MU-AC9 and MU-AC18 both explicitly name
# "actor" as a required audit-row field, and neither test asserted it
# (the field was written correctly, just never proven). Fixed same-day
# with 4 new assertions across 4 existing tests (2 unit, 2 integration)
# - no new test functions needed. 3 Low disclosed Missing-Edge-Case
# findings (2 last-admin boundary unit tests, 1 concurrent-PATCH race
# test, all matrix-planned but never built) noted non-blocking, since
# none leaves a numbered AC's own text unproven. No spec drift found.
# Gate re-confirmed green: 509/509 tests, 96.64% coverage. Advances to
# PR.
# SECURITY_REVIEW done 2026-09-02: docs/security/US-011-security-
# review.md - Pass. All 6 AGENTS.md SS7 rows clean (3 N/A - story has
# no password/credential handling at all, creation is invitation-only
# and structurally cannot accept a password field). 2 Low advisories
# noted (invitation-completion flow not yet built anywhere, out of this
# story's scope; resend's authz_denied audit lives in the shared
# require_scope dependency, not admin_users itself - confirmed correct,
# not a gap).
# VERIFICATION done 2026-09-02: docs/verification/US-011-verification-
# report.md - Pass. 1 real gap found+fixed same-day: 5 of 7 protected
# routes had only a missing_token security case at the integration
# level, not the full AGENTS.md SS5 five-case set (expired/malformed/
# insufficient-permission/revoked). 24 new integration tests added, gate
# re-confirmed green: 509/509 tests (was 485), 96.64% coverage. ORM
# containment, cache TTL, service->service discipline, response_model/
# extra=forbid/.env.example/no-sensitive-Read-field all confirmed Pass
# on first read.
# IMPLEMENTATION done 2026-09-02 (T1-T9 complete). gate-enforcer PASS:
# 7/7 pre-commit hooks (ruff lint+format, mypy strict on 113 files,
# import-linter 6/6, unit tests, no-mock-in-integration, detect-secrets),
# 485/485 tests (281 unit, 204 integration - was 409 total before this
# story), 96.64% coverage (floor 85%; admin_users/service.py 96%,
# admin_users/router.py 100%). Migrations a5edc35c8e96 +
# 1b2b1d52dd71 upgrade/downgrade/upgrade proven (T3/T3b). New module
# app/modules/admin_users/ (7 endpoints). 2 real defects found+fixed
# during T7/T8 (not caught by planning/plan-review):
# (1) genuine concurrency bug - raise_if_last_admin's shared last-admin
# count query locked only user_roles rows (FOR UPDATE OF UserRole),
# providing no serialization against a concurrent users.status update;
# two simultaneous deactivations of the last two admins both returned
# 200 until fixed to lock both UserRole and User rows
# (roles/repository.py count_active_admins_excluding) - this also
# benefits US-3.2's existing replace_user_roles caller, still green;
# (2) 2 of this story's own new concurrency-test helpers didn't clean up
# users seeded via the raw-engine/real_client pattern, causing the
# roles module's own pre-existing concurrency test to intermittently
# fail when run afterward (leftover active admins inflated its count) -
# fixed via cleanup_users. ORM containment, cross-module discipline,
# response_model/status_code on every route, extra="forbid" on every
# inbound schema all confirmed clean. Added 2 new app/core/config.py
# settings + .env.example entries; EmailSender protocol gained
# send_invitation_email (3 existing test fakes updated). Advancing to
# VERIFICATION.
# TESTS done 2026-09-02: docs/tests/US-011-traceability-matrix.md (matrix
# only, test code deferred to IMPLEMENTATION T7/T8 per T1-T9 breakdown).
# All 21 source ACs + FR-17b/FR-22/FR-23 (no-AC) covered at unit and/or
# integration level; MU-AC16/BR-001 both have dedicated concurrency
# tests; roles/service.py's 2 new methods get independent unit coverage
# plus 2 regression-guard tests proving the plan-review-corrected wiring
# (check_no_privilege_escalation extracted+called, raise_if_last_admin
# additive-only+not called) holds. Advancing to IMPLEMENTATION.
# PLANNING done 2026-09-02: docs/impact-analysis/US-011-impact-analysis.md
# (new module app/modules/admin_users/, mirrors profile/roles precedent;
# 2 new cross-module RoleService calls flagged). docs/plans/US-011-
# implementation-plan.md (resolves PATCH's missing reason field, FR-18's
# token-invalidation mechanism, adds 2 new RoleService methods).
# docs/plans/US-011-task-breakdown.md (T1-T9). docs/reviews/plans/
# US-011-plan-review.md - Pass with Issues, 4 findings found+fixed same-
# day: T3's migration bundled a CONCURRENTLY index with transactional
# DDL (split into T3/T3b per US-2.6 precedent); migrations/env.py touch
# was assigned outside its declared skill scope (now unassigned/out-of-
# band per US-3.2 precedent); advisor cross-check caught a genuine
# behavior-change bug (raise_if_last_admin wrongly wired into
# replace_user_roles, would have rejected a role change that succeeds
# today - fixed to additive-only); 2 smaller design clarifications
# closed (admin_audit_log.actor_id mapping; DELETE 401-vs-405 reading).
# Branch corrected mid-session: work had started on stale
# feat/us-2.6-active-sessions, re-based onto fresh feat/us-3.1-manage-
# users off origin/main via git stash (clean, no conflicts). See
# docs/catalog/US-3.1-pipeline-status.md for the full stage-by-stage
# record. Advancing to TESTS.
# DESIGN done 2026-09-02: docs/designs/api/US-011-{openapi.yaml,api-
# design.md} (7 operations across 6 paths; ETag/If-Match mechanics reused
# verbatim from app/core/etag.py + app/modules/profile's precedent,
# previously unavailable to US-3.2; 9 Open Questions logged, none
# blocking). docs/designs/database/US-011-{db-design,entity-model}.md
# (users: no schema change, 2 new indexes incl. first-ever pg_trgm use in
# this codebase; new invitation_tokens table matching existing token
# precedent; admin_audit_log gains 4 nullable columns per OD-1;
# account_lifecycle_audit_log gains 1 nullable column per OD-2; 3 Known
# Gaps logged, incl. PATCH request body missing a reason field for FR-9's
# new admin_audit_log.reason column). Advancing to PLANNING.
# SPEC_REVIEW done 2026-09-02: docs/reviews/specifications/US-011-spec-
# review.md - Pass with Issues. 21/21 ACs Covered, no contradictions. 3
# Low findings, all disclosed/precedent-grounded (FR-6's BR-001
# concurrency sentence; FR-17b/FR-22/FR-23 lacking a source AC, matching
# US-2.6 FR-6/FR-7 precedent). User accepted as-is 2026-09-02.
# SPECIFICATION done 2026-09-02: docs/specifications/US-011-manage-
# users-spec.md revised in place (pre-existing 2026-08-22 draft).
# Incorporates OD-1 (admin_audit_log gains nullable field/old_value/
# new_value/reason columns for FR-9), OD-2 (account_lifecycle_audit_log
# gains nullable reason column for FR-13), OD-3 (new FR-22/FR-23 for
# GET /v1/admin/users/{id}, no source AC, mirrors US-2.6 FR-6/FR-7
# precedent), plus a new FR-17b (Deactivate unknown-user 404, precedent
# from FR-12/FR-21). 21/21 source ACs traced; FR-17b/FR-22/FR-23 have no
# AC (Open-Decision-derived, per template rules). Out-of-Scope wording
# ambiguity (spec-review Low finding) fixed. Advancing to SPEC_REVIEW.
# Prior CLARIFICATION record below.
# CLARIFICATION done 2026-09-02: us-clarifier verdict Not Ready. 3 Open
# Decisions logged (docs/decisions/US-3.1-open-decisions.md) - OD-1
# admin_audit_log's existing US-3.2-built shape (old_roles/new_roles
# arrays) doesn't fit MU-AC9/FR-9's per-field Update audit requirement;
# OD-2 account_lifecycle_audit_log (US-1.4-built) has no reason column
# but MU-AC13/FR-13 requires persisting an admin-supplied reason; OD-3
# GET /v1/admin/users/{id} is in-scope but has zero Acceptance Criteria.
# 6 of the pre-existing US-011 spec's/review's open items resolved
# outright by reading the real US-1.4 (account module)/US-3.2 (roles
# module) codebase merged since the 2026-08-22 draft: concurrent
# duplicate-email (BR-001 atomic constraint), role->permission mapping
# for MU-AC8 (RoleService.replace_user_roles's existing privilege-
# escalation check), Deactivate's missing unknown-user case (precedent
# from FR-12/FR-21's 404), invitation-token shape (matches
# EmailVerificationToken/PasswordResetToken precedent), resend rate-
# limit mechanism (matches email_verification's cooldown pattern),
# last-admin check (RoleService.count_active_admins_excluding). See
# docs/evidence/US-3.1-clarification-report.md. Awaiting user resolution
# of OD-1/OD-2/OD-3 before advancing to SPECIFICATION.

# --- prior story: US-2.6 (DONE, reached PR, merged) ---
# story: US-2.6
# current_stage: PR
# status: DONE
# PR done 2026-09-02: docs/pr/US-010-pr-description.md drafted by
# pr-preparer. All 4 gates confirmed Pass from their actual reports
# (gate-enforcer 7/7 + 409/409 tests/97.42% coverage; implementation-
# verifier Pass; security-reviewer Pass; reconciliation-reviewer Pass).
# .env.example alignment confirmed (2 new settings, 1:1 with config.py).
# Commit hygiene confirmed via git status - every changed/new file traces
# to this story's scope. User explicitly authorized commit+push+PR.
# Committed as 2a749fd, pushed to origin, opened as GitHub PR #11
# (https://github.com/AndriiDobrovolskii/Customer_Portal/pull/11).
# Started 2026-09-02. US-2.5 reached PR #10, confirmed MERGED to main.
# US-2.6 (Active Sessions) has no dependency on US-2.5's MFA work - only
# on US-2.3's refresh-token family_id (on main) and US-2.2's revocation
# path (on main), both already merged - no blocking platform dependency.
# us-clarifier ran 2026-09-02: initially Not Ready, 4 Open Decisions
# logged (docs/decisions/US-2.6-open-decisions.md) - own-current-session
# revocation behavior, 20-family cap eviction mechanics, device/browser
# label derivation, geo-IP location mechanism. 3 of the pre-existing
# US-010 spec's 6 Open Questions resolved outright by reading US-2.3's
# actual shipped implementation (metadata capture + last_used_at write
# strategy already built; malformed family_id resolved by precedent).
# All 4 ODs resolved by the user 2026-09-02, recommended option accepted
# throughout: OD-1 409 on self-revoke; OD-2 evict oldest + audit
# (event=session_evicted), no email; OD-3 `user-agents` library,
# "{browser} on {OS}" format, "Unknown device" fallback; OD-4 local
# MaxMind GeoLite2-City DB, no live network call. See
# docs/evidence/US-2.6-clarification-report.md. Advances to
# SPECIFICATION.
# SPECIFICATION done 2026-09-02: docs/specifications/US-010-active-
# session-management-spec.md revised in place (pre-existing 2026-08-22
# draft). Incorporates OD-1-OD-4 plus 2 new FRs (FR-6 self-revoke 409,
# FR-7 cap eviction) with no source AC. 5/5 source ACs traced.
# SPEC_REVIEW done 2026-09-02: docs/reviews/specifications/US-010-spec-
# review.md - Pass with Issues (2 Medium findings found+fixed same-day).
# 5/5 ACs Covered, no contradictions. Current-session identification
# mechanism (undefined for FR-1/FR-6) and concurrent-login cap-eviction
# race (same class of bug already hit in US-3.2) both resolved by user:
# read refresh_token cookie; row-lock the count-and-evict check. 4
# disclosed Low scope-creep findings noted, non-blocking.
# DESIGN done 2026-09-02: docs/designs/api/US-010-{openapi.yaml,api-
# design.md} (2 endpoints, 1 new error slug current-session/409).
# docs/designs/database/US-010-{db-design,entity-model}.md (no new
# refresh_tokens columns - all already exist from US-2.1/2.2/2.3; 1 new
# composite index for per-family aggregate queries; 1 new column
# auth_audit_log.target_family since reason/scope were wrong size/
# semantics; rejected a first-class refresh_token_families table as
# scope beyond the story).

# --- prior story: US-2.5 (DONE, reached PR, merged) ---
# story: US-2.5
# current_stage: PR
# status: DONE
# PR done 2026-09-02: docs/pr/US-009-pr-description.md drafted by
# pr-preparer. All 4 gates confirmed Pass from their actual reports
# (gate-enforcer 7/7 + 367/367 tests/97.35% coverage; implementation-
# verifier Pass; security-reviewer Pass; reconciliation-reviewer Pass).
# .env.example alignment confirmed (4 new settings, 1:1 with config.py).
# Commit hygiene confirmed via git status - every changed/new file traces
# to this story's scope, no drive-by refactor. User explicitly authorized
# push+PR ("gp"). One detect-secrets false positive fixed first (a
# pre-existing "WrongPassword1!" literal shifted line numbers due to this
# session's own edits, re-flagged; pragma: allowlist secret added).
# Committed as ae8d2a2, pushed to origin, opened as GitHub PR #10.
# RECONCILIATION done 2026-09-02: docs/reconciliation/US-009-reconciliation-report.md
# - Pass (7 gaps found+fixed same-day). All 7 source ACs + FR-8 have full,
# verified matrix coverage. Initial pass found 7 test functions the
# pre-implementation traceability matrix names did not exist under any
# name (a further ~dozen rows were renamed-but-equivalent, individually
# verified, not counted as gaps). None corresponded to a live bug - the
# two allow-list gaps were independently re-checked against router.py and
# found correctly wired before their tests were written. Closed same-day,
# 6 new tests (1 integration/roles, 3 integration/users, 2 unit/users):
# (1) most consequential - test_replace_user_roles_sets_granted_at_to_
# recent_value now proves replace_for_user's granted_at write against a
# real DB (FR-6's grace-period-clock data source); (2) test_enroll_mfa_
# accepts_enrollment_scoped_token; (3) test_mfa_disable_rejects_
# enrollment_scoped_token (also closes gap 7's partial-coverage concern -
# a second concrete allow-list data point); (4) test_verify_mfa_wrong_
# recovery_code_counts_toward_totp_lockout (OD-10); (5) test_rotate_
# refresh_token_both_scoping_triggers_true_issues_single_scoped_token;
# (6) test_verify_mfa_replayed_code_rejected_against_real_valkey (replay
# protection proven against real Valkey, not just a fake). No AC-level
# spec drift found; the disable_mfa exception-type change from
# SECURITY_REVIEW was checked against the OpenAPI design and confirmed to
# match its already-generic "cross-cutting shape, reused" contract, not a
# divergence. Gate re-confirmed green: 7/7 pre-commit, mypy clean, 367/367
# tests (was 361), 97.35% coverage. Advances to PR.
# SECURITY_REVIEW done 2026-09-02: docs/security/US-009-security-review.md -
# Pass (1 finding found+fixed same-day). 5/6 AGENTS.md SS7 rows clean on
# first read; rule 2 (no reversible encryption for credentials) required
# explicit reasoning - mfa_secret_encrypted's AES-GCM envelope encryption
# is correct, not a violation, because a TOTP secret must be recomputed-
# from to verify a code (no one-way hash is possible), unlike recovery
# codes (Argon2id-hashed, compare-only, correctly matching rule 1). Rule 6
# (uniform auth-failure response) surfaced a genuine defect: disable_mfa
# checked password then code sequentially, raising a different exception
# per factor (InvalidCredentialsError vs MfaInvalidCodeError) - since the
# code check only ran after the password check passed, the distinct
# mfa-invalid-code response already confirmed the password was correct, an
# oracle a stolen bearer session could use to brute-force each factor
# independently (this endpoint's whole purpose is joint two-factor
# defense against exactly that threat model). Fixed same-day:
# app/modules/users/service.py's disable_mfa now evaluates both factors
# unconditionally (no short-circuit, no timing signal) and raises one
# exception type on either failure; regression-guarded by a new
# indistinguishability test. Full gate re-run green: 7/7 pre-commit hooks,
# mypy strict clean, 361/361 tests (was 360 - added 1), 97.35% coverage.
# 3 Low advisory findings noted, none forcing a Fail.
# VERIFICATION done 2026-09-02: docs/verification/US-009-verification-report.md
# - Pass. 1 real gap found+fixed same-day: 13 missing AGENTS.md SS5
# security-case tests across enroll/activate/disable (activate/disable
# had none at all) plus verify's scheme-specific equivalents. All added,
# full suite now 360/360, gate re-confirmed green. ORM containment,
# cross-module discipline, cache TTLs, response_model/extra=forbid all
# confirmed Pass; eager-loading N/A (no relationship introduced).
# IMPLEMENTATION done 2026-09-01 (T1-T9 complete). gate-enforcer PASS:
# 7/7 pre-commit hooks (ruff lint+format, mypy strict, import-linter 6/6,
# unit tests, no-mock-in-integration, detect-secrets), 347/347 tests
# (211 unit + 136 integration), 97.35% coverage (floor 85%; service.py
# 98%, router.py 100%). Migration cef55228a927 upgrade/downgrade/upgrade
# proven (T3). ORM containment, cross-module discipline, cache TTLs,
# banned-idiom grep, response_model/status_code on every route all
# confirmed clean. Added `cryptography` as a new pyproject.toml
# dependency (AES-GCM for OD-2's secret-at-rest encryption).
# IMPLEMENTATION in progress 2026-09-01, T1-T6 done per
# docs/plans/US-009-task-breakdown.md:
# T1 schemas (users/schemas.py: 8 new MFA schemas, LoginResponse/
#   RefreshResponse gain mfa_enrollment_deadline).
# T2 users data layer (models.py: 4 new User columns + MfaRecoveryCode;
#   repository.py: 8 new methods; cache.py: MfaTokenCache/MfaReplayCache;
#   cache_keys.py: 3 new keys - moved here from planned T5 per US-008's
#   actual precedent, since cache.py is their first consumer).
# T2b roles data layer (models.py: UserRole.granted_at; repository.py:
#   list_role_grants_for_user, replace_for_user explicit granted_at write).
# T3 migration cef55228a927 - upgrade/downgrade/upgrade proven against
#   real Postgres, schema confirmed via psql.
# T4 roles service (get_role_grants_for_user + RoleGrant NamedTuple).
# T5 users service + core primitives - new app/core/crypto.py (AES-GCM,
#   added `cryptography` as a new pyproject.toml dependency via uv),
#   TOTP primitives added to security.py, 4 new config settings, 4 new
#   MFA service methods (enroll/activate/verify/disable), default-deny
#   enrolment-scoping check added to get_authenticated_user, login/
#   refresh both re-evaluate scoping via new _resolve_enrollment_scoping/
#   _encode_access_token_for_user helpers. 3 new exceptions. New
#   EmailSender.send_mfa_recovery_used_notice.
# T6 users router + dependencies - 4 new routes, login route's response
#   became LoginResponse | MfaRequiredResponse with conditional cookie,
#   new CurrentUserAllowEnrollmentScopedDep mirroring
#   get_current_user_allow_revoked. All routes confirmed live via
#   app.openapi().
# Remaining: T7 (unit tests), T8 (integration tests), T9 (gate-enforcer).
# TESTS done 2026-09-01: docs/tests/US-009-traceability-matrix.md (matrix
# only, test code deferred to IMPLEMENTATION T7/T8 per T1-T9 breakdown).
# All 7 ACs + FR-8's no-AC disable path covered at unit+integration;
# dedicated coverage for the default-deny enrolment-scoping mechanism
# (plan's flagged highest-risk line) in both directions plus 2 full-flow
# integration tests (one per trigger).
# PLANNING done 2026-09-01: docs/impact-analysis/US-009-impact-analysis.md
# (extends users module, no new module; 2 open architectural tensions
# flagged - enrolment-scoped-token mechanism, mfa_token mechanism).
# docs/plans/US-009-implementation-plan.md resolves both tensions (JWT
# claim + default-deny at get_authenticated_user mirroring
# get_current_user_allow_revoked; Valkey-backed opaque mfa_token via
# GETDEL). docs/plans/US-009-task-breakdown.md (T1-T9, roles-module data/
# service work split into T2b/T4 per US-012's app/core/* placement
# precedent). docs/reviews/plans/US-009-plan-review.md - Pass with
# Issues (1 Low traceability nit, 1 Medium concurrency-risk gap), both
# resolved same-day and folded into the plan.
# DESIGN done 2026-09-01: docs/designs/api/US-009-{openapi.yaml,api-design.md}
# (5 endpoints: enroll/activate/verify/disable new, login/refresh extended
# per FR-6; 4 Open Questions deferred to PLANNING - activate's error
# distinction, disable's recovery-code acceptance, disable's check-order,
# grace-period field naming). docs/designs/database/US-009-{db-design,
# entity-model}.md (4 new users columns, new mfa_recovery_codes table,
# 1 new column - granted_at - on the already-merged US-3.2 user_roles
# table; enrolment state derived, not a new enum; FR-8's hard-delete
# deliberately diverges from PasswordResetToken's keep-but-mark pattern).
# SPEC_REVIEW done 2026-09-01: docs/reviews/specifications/US-009-spec-review.md
# - Pass with Issues. 7/7 ACs Covered, no contradictions. 1 High finding
# (no data source for the 14-day grace-period clock - user_roles had no
# timestamp column) + 2 Medium ambiguities (recovery-trigger grace scope,
# refresh-while-scoped behavior), all resolved by the user 2026-09-01
# (recommended options accepted throughout: add user_roles.granted_at
# column; no grace period for the recovery-code trigger; refresh
# re-evaluates and re-scopes). Folded into the spec (FR-2/FR-6) plus a
# new Open Decision Resolutions bullet. 3 Low scope-creep findings
# (mfa_reenrollment_required flag, perm_epoch/token-stale reuse, FR-8's
# no-AC disable-success path) noted as disclosed/user-approved, non-
# blocking.
# SPECIFICATION done 2026-09-01: docs/specifications/US-009-mfa-totp-spec.md
# revised in place (pre-existing 2026-08-22 draft) to incorporate all 11
# resolved Open Decisions as an "Open Decision Resolutions" section, plus
# 2 new FRs not derivable from any single AC (FR-6 folds in the
# enrolment-scoped-token mechanism per OD-4/OD-7, now with two triggers
# per OD-5; FR-8 is wholly new, covering the previously-uncovered
# DELETE /v1/auth/mfa success path per OD-6/OD-8). All 7 source ACs
# traced (MF-AC1-7 -> FR-1..FR-7); FR-8 has no AC (Open-Decision-derived,
# per template rules). Open Questions section: none remaining.
# CLARIFICATION done 2026-09-01 (re-run): OD-1 (blocking) resolved - US-3.2
# reached PR #9 (open/unmerged, every stage Pass), role/scope system
# confirmed live matching BR-010/011/013. A pre-existing draft spec
# (US-009) and review already existed (2026-08-22) - did not resolve
# OD-2/3/4/5/6/7 by precedent (unlike US-008 for US-2.4) but corroborated
# them, and surfaced OD-8 (disable-success path, "[High]" in the review),
# OD-9 (recovery exhaustion), OD-10 (recovery-vs-lockout counter), OD-11
# (re-enroll-while-pending). New OD-7: MF-AC6's "US-3.2 MR-AC1" citation
# is fictional; resolved with a perm_epoch exit condition, coupled to
# OD-4. All 11 ODs resolved by the user 2026-09-01 (see
# docs/decisions/US-2.5-open-decisions.md Resolutions Summary) -
# recommended options accepted throughout except OD-5, where the user
# chose the non-default "degraded re-enrolment-required state"
# (mfa_reenrollment_required flag), generalizing OD-7's enrolment-scoped-
# token mechanism into a two-trigger primitive. Process decision: merge
# US-3.2 PR #9 first, then branch feat/us-2.5-mfa-totp from main (not
# stacked) - avoids the US-2.4 PR #7 auto-close trap. Merging PR #9 not
# yet done - awaiting explicit user instruction.

# --- prior story: US-3.2 (DONE, reached PR) ---
# story: US-3.2
# current_stage: PR
# status: DONE
# Every stage Pass through PR draft (see docs/pr/US-012-pr-description.md).
# gate-enforcer PASS: 7/7 hooks, mypy strict clean (94 files), lint-imports
# 6/6, 281/281 tests, 97.27% coverage. VERIFICATION: Pass, 1 SS5 gap
# found+fixed (4 new security-case tests). SECURITY_REVIEW: Pass, 4/6 SS7
# rows N/A/Pass, 3 Low advisories. RECONCILIATION: Pass, 2 gaps
# found+fixed (stale matrix, MR-AC1 perm_epoch assertion). New
# app/modules/roles/ module; cross-cutting changes to core/security.py
# (scopes claim), core/revocation_cache.py (PermissionEpochCache),
# users/service.py (perm_epoch check, token-stale). 2 real bugs
# found+fixed during implementation: (1) a genuine FR-7 concurrency race
# (no row locking), fixed with SELECT...FOR UPDATE; (2) a pre-existing
# flaky test, made deterministic. One user-approved exception:
# migrations/env.py gained one model-registration import line, matching
# the identical pattern used for every prior new module. Not yet
# committed/pushed - awaiting explicit user instruction. US-2.5
# (MFA/TOTP) remains parked/BLOCKED, ready to resume once this branch
# merges. See docs/workflow/history.jsonl for the full T1-T9 substep
# trail. Committed as fb70f66+795223b, moved onto a proper feature branch
# (feat/us-3.2-manage-roles) after being caught committed directly to
# main by mistake - local main reset to match origin (safe, nothing had
# been pushed yet, both commits preserved). Pushed and opened as GitHub
# PR #9.

# --- prior story: US-2.5 (BLOCKED, parked) ---
# story: US-2.5
# current_stage: CLARIFICATION
# status: BLOCKED
# us-clarifier ran 2026-09-01: Not Ready. 6 Open Decisions logged
# (docs/decisions/US-2.5-open-decisions.md). OD-1 (BLOCKING - US-3.2
# dependency) resolved 2026-09-01: user chose to build US-3.2 first.
# Resume US-2.5 and re-run clarification once US-3.2 reaches PR/merge.

# --- prior story: US-2.4 (DONE) ---
# story: US-2.4
# current_stage: PR
# status: DONE
# Every stage Pass through PR (see docs/catalog/US-2.4-pipeline-status.md).
# Committed (c973548) and opened as GitHub PR #7, stacked on #6
# (feat/us-2.3-refresh-token). #6 merged first (28c1f5a); GitHub then
# auto-closed #7 and permanently refused to reopen it once its base
# branch was deleted - replaced by PR #8 (same branch/commit, retargeted
# to main), which merged cleanly as 800a405. Both US-2.3 and US-2.4 are
# now on main - confirmed via `git log`, 2026-09-01. gate-enforcer PASS: 7/7 pre-commit hooks, mypy strict clean (82
# files), import-linter clean, 244/244 tests, 97.12% coverage (floor 85%;
# service.py 98%, router.py 100%), migration cycle proven. VERIFICATION:
# Pass. SECURITY_REVIEW: Pass, all 6 AGENTS.md SS7 rows Pass (including a
# reasoned PR-AC4 token-state-split analysis), 3 Low advisories
# (disclosed/pre-existing). RECONCILIATION: Pass, 2 matrix-accuracy gaps
# found+fixed same-day, 1 Low Spec Drift finding disclosed (unverified-
# account eligibility exclusion, no external contract change).
# CLARIFICATION done 2026-09-01: initially Not Ready (3 Open Decisions -
# breached-password check mechanism, request-throttle check order,
# request-side audit logging), all resolved by the user (recommended
# option accepted throughout): OD-1 local static breached-password
# list/bloom filter (no live network call), OD-2 check order cooldown ->
# per-account/hour -> per-IP/hour, OD-3 request endpoint also writes
# auth_audit_log (event=password_reset_requested) on every attempt
# including unknown/deactivated accounts. A pre-existing spec (US-008,
# drafted 2026-08-22, predates the real US-2.1/2.2/2.3 codebase) already
# existed; re-reading it resolved its own 2 unresolved Open Questions by
# direct precedent (email_verification's token-state -> error-type
# mapping; LoginRequest's unvalidated email: str). See
# docs/decisions/US-2.4-open-decisions.md,
# docs/evidence/US-2.4-clarification-report.md.

# --- prior story history retained for context ---
# US-2.3 (Refresh Token, Epic 2) reached PR with every stage Pass (see
# docs/catalog/US-2.3-pipeline-status.md), was committed (c0fc4d8) and
# opened as GitHub PR #6 (state: OPEN, unmerged) - confirmed via `gh pr
# list` and `git log`, 2026-09-01. This file's tracking of US-2.3 was
# stale (never updated when the story actually completed in a prior
# session) and was backfilled 2026-09-01 before advancing to US-2.4.
# US-2.2 (Logout, Epic 2) reached PR with every stage Pass (post-PR IDOR
# fix included), was committed and merged as PR #5 on GitHub (commit
# 157fa36, merge 76e70e8) - confirmed via git log on main, 2026-09-01.
# US-001, US-002, US-003 are implemented per git history (commits b6dc29e,
# ed6e2a9, 90a612b, 1f8d2a5, 1ee7dc5), though not tracked through this
# pipeline. US-004 (Deactivate Account, Epic 1 / "1.4") completed the full
# pipeline through PR, was committed (e3b42c4) and merged as PR #1 on
# GitHub (confirmed via `gh pr view 1`, state: MERGED, 2026-08-31).
# US-2.1 (Login, Epic 2) completed the full pipeline through PR (see
# docs/catalog/US-2.1-pipeline-status.md, all stages Pass), was committed
# and merged as PR #2 on GitHub (commit 1ecedb0, merge d14a51b).
