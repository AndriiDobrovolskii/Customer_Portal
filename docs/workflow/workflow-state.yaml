# Execution state of the active delivery workflow.
# Schema: docs/workflow/state-schema.md
# Written by story-orchestrator ONLY. Stage skills return a Result Envelope;
# the orchestrator records the transition here and appends one event to
# docs/workflow/history.jsonl.
#
# This file is structured data, not a narrative log. Rationale belongs in the
# stage's own artifact (a clarification report, a review, an open-decisions
# entry) or in history.jsonl. The pre-migration version of this file carried
# ~53KB of free-form session commentary; it is preserved verbatim in git at
# commit 6af0040, and the per-story rollups under docs/catalog/ remain.

version: 1

story: US-4.2
workflow: story-delivery

current_stage: ARCHIVED
previous_stage: COMPLETED
last_completed_stage: COMPLETED

status: ARCHIVED
attempt: 1

last_invoked_skill: pr-preparer

last_result:
  verdict: PASS
  stage: PR_PREPARATION
  recorded_at: "2026-09-06T02:15:00Z"

last_artifacts:
  - type: pr_summary
    path: docs/pr/US-4.2-pr-summary.md
    version: 1

implementation_substep: null

pending_human_gate: null

blocking_issues: []

non_blocking_findings:
  - "PR_PREPARATION (PASS): .gitignore's addition of docs/knowledge has no traceable connection to this story's spec/plan/impact-analysis - flagged for confirmation before merge, not blocking (working-tree hygiene concern, not a gate finding). docs/pr/US-4.2-pr-summary.md v1."
  - "TEST_WRITING (v2, PASS, re-entry): implementation_plan/task_breakdown/plan_review advanced v1->v2 (Architectural Change #12, app_runtime role) while this story sat at TEST_WRITING - test-writer re-verified rather than regenerated: grep count match (60/42/12 test functions) confirms zero test file drift from v1, since implementation_plan v2's own Testing Strategy section states the unit suite is unaffected and the integration suite needs no rewrite. test_strategy/ac_test_matrix/test_generation_report bumped to v2 for input provenance only. docs/tests/US-4.2-test-strategy.md v2."
  - "TEST_WRITING (v2, PASS): implementation_plan v2 Risk 7 / Testing Strategy adds a full-suite regression re-run requirement (every other module's existing integration tests, once tests/conftest.py serves app_runtime) as proof the new role's GRANT list is complete - this is IMPLEMENTATION T4/T7's (migration-manager / gate-enforcer) responsibility per task_breakdown v2, not new test-writer content. docs/evidence/US-4.2-test-generation-report.md v2."
  - "PLAN_REVIEW (v2, PASS): Architectural Change #12's four files (scripts/db/provision_runtime_role.sql, app/main.py, tests/conftest.py, app/core/config.py's runtime_database_url) have no impact_analysis v2 origin - traced instead to the HUMAN_REDIRECTED transition (history.jsonl, 2026-09-05T18:30:00Z) and TR-AC3 itself. Not scope creep; plan-reviewer recommends impact_analysis be refreshed on a future story touching this area for retrospective consistency. docs/reviews/plans/US-4.2-plan-review.md v2."
  - "PLAN_REVIEW (v2, PASS): T4 (migration-manager) and app/main.py folded into T5 (service-and-router-builder) both exceed those skills' usual footprint - judged acceptable (domain overlap for T4's PostgreSQL role/DDL provisioning; precedent from this story's own v1 for T5's config-line fold). If disputed, correct loop-back is changes_required_sequencing (stays within IMPLEMENTATION_PLANNING), not ARCHITECTURE_PLANNING. docs/reviews/plans/US-4.2-plan-review.md v2."
  - "IMPLEMENTATION_PLANNING (v2, PASS): task_breakdown v1 was stale (referenced implementation_plan v1; implementation_plan v2 added Architectural Change #12 - the app_runtime role). Regenerated as v2. Assigned scripts/db/provision_runtime_role.sql + tests/conftest.py's fixture change to migration-manager (new T4, depends on T3) and app/main.py's one-line lifespan change to service-and-router-builder (folded into T5) - both assignments are wider than those skills' usual file footprint since implementation_plan v2 explicitly deferred the assignment to this stage rather than naming an owning skill itself. Flagged for plan-reviewer and HUMAN_PLAN_APPROVAL to weigh in; correct loop-back if disputed is changes_required_sequencing (stays within IMPLEMENTATION_PLANNING), not another ARCHITECTURE_PLANNING pass. docs/plans/US-4.2-task-breakdown.md v2."
  - "IMPLEMENTATION_PLANNING (v2, PASS): docs/catalog/US-4.2-pipeline-status.md v2 still records task_breakdown v1 as its input - stale once v2 lands. Not this stage's output; surfaced for IMPLEMENTATION's re-entry (T4 onward) to update pipeline_status rather than being discovered mid-composite."
  - "IMPLEMENTATION T3 (migration-manager, BLOCKED): DR-2 (attachments.ticket_reply_id index CONCURRENTLY question) is now resolved, not open - plain CREATE INDEX, since US-4.1 shipped attachment binding without an upload endpoint so the table has no production write path populating it yet. See migrations/versions/9132a68b73c8_add_ticket_replies.py."
  - "IMPLEMENTATION T3 (migration-manager, BLOCKED): revision 9132a68b73c8 is applied to the local dev database and present in the working tree, uncommitted. Suite red-state character changed - the two direct RLS integration tests now fail on assertion (not the expected pre-IMPLEMENTATION collection ImportError) because of the blocker above, not an implementation bug; a later QUALITY_GATE run needs this context."
  - "IMPLEMENTATION_PLANNING (v1, PASS): one item remains deferred to execution time per the plan's own note - the final TicketService/TicketReplyService file-internal split (T4, service-and-router-builder's call) - docs/plans/US-4.2-task-breakdown.md"
  - "TEST_WRITING (v1, PASS): test-writer's own collaborator-shape assumption - a new TicketReplyService class (not new methods on TicketService), with a single actor_kind: Literal['customer','agent'] parameter reused across create_reply and get_ticket_detail, derived per implementation-plan Architectural Change #2's tickets:write check for both routes. If service-and-router-builder (T4/T5) ships a different shape, a reconciliation TEST_WRITING pass is needed, mirroring US-4.1's own v2 reconciliation pass - docs/tests/US-4.2-test-strategy.md"
  - "TEST_WRITING (v1, PASS): API_DESIGN OQ-2's POST-side 'agent-shaped but missing tickets:write' combination is unit-tested only, not reachable via real HTTP under the shipped role seed - docs/tests/US-4.2-ac-test-matrix.md"
  - "TEST_WRITING (v1, PASS): all reply-related tests are red (ImportError) until IMPLEMENTATION lands the missing app/modules/support symbols - expected TDD state, not a regression; adding these imports also breaks collection of the 12/22 pre-existing US-4.1 tests in the same two files until T1/T2/T4 land, self-resolving once IMPLEMENTATION completes - docs/evidence/US-4.2-test-generation-report.md"
  - "ARCHITECTURE_PLANNING (v2, PASS): implementation_plan v2 adds Architectural Change #12 (dedicated non-superuser app_runtime DB role) - widens this story's file footprint to app/main.py and tests/conftest.py (outside app/modules/support/ and app/core/), and requires a full-suite regression pass (not just support's tests) once tests/conftest.py switches to the new role, since every module now runs its requests through app_runtime. New operational artifact scripts/db/provision_runtime_role.sql is not an Alembic migration and its per-environment execution/documentation is out of this plan's scope (flagged for documentation-and-adrs). tests/conftest.py's fixture change doesn't fit any of the four IMPLEMENTATION builder skills' usual footprint - IMPLEMENTATION_PLANNING must assign it explicitly. docs/plans/US-4.2-implementation-plan.md v2."

started_at: "2026-09-04T16:30:00Z"
updated_at: "2026-09-06T05:00:00Z"
completed_at: "2026-09-06T02:30:00Z"
archived_at: "2026-09-06T05:00:00Z"

note: >
  plan-reviewer reviewed implementation_plan v1 and task_breakdown v1 against
  impact_analysis v2, specification v6, api_design v3/openapi v3, and
  db_design v3/entity_model v3. Full impact-analysis coverage (every affected
  file/decision item traced to a plan section or Files-To-Modify row),
  correct AGENTS.md section 3 task-breakdown ordering (T1/T2 parallel, T3
  migration before T4/T5 service/router, matching the migration-before-
  model-use rule), concrete risk and test-strategy sections aligned with
  AGENTS.md sections 4/5, and no untraceable scope creep. No blocking or
  non-blocking findings - PASS. docs/reviews/plans/US-4.2-plan-review.md v1.
  Advanced to HUMAN_PLAN_APPROVAL - awaiting /so:approve or /so:reject.

  HUMAN_PLAN_APPROVAL: human approved via /so:approve at 2026-09-05T16:30:00Z
  (decided_by: sbruhov@gmail.com). Required artifacts implementation_plan v1,
  task_breakdown v1, plan_review v1 all current with recorded verdict PASS
  and no blocking issues. Advanced to TEST_WRITING.

  TEST_WRITING: test-writer wrote docs/tests/US-4.2-test-strategy.md v1,
  docs/tests/US-4.2-ac-test-matrix.md v1, docs/evidence/US-4.2-test-
  generation-report.md v1 against specification v6, openapi v3,
  entity_model v3, impact_analysis v2, implementation_plan v1,
  task_breakdown v1, plan_review v1. Added tests/unit/modules/support/
  test_support_schemas.py (new, 12 tests), extended test_support_service.py
  (+30 tests, additive-only fake extensions) and test_support_router.py
  (+38 tests, incl. 3 direct RLS tests and a statement-count-independence
  test), covering every TR-AC1-TR-AC7 plus the carried API_DESIGN OQ-1
  no-op branch and Risk 6's rate-limit key independence. ruff check/format
  clean; py_compile clean; pytest --collect-only fails only on ImportError
  against not-yet-built app/modules/support symbols (expected pre-
  IMPLEMENTATION red state - see test-generation-report). No application
  code changed. PASS, no blocking issues. Advanced to IMPLEMENTATION.

  IMPLEMENTATION T1 (schema-builder, PASS): app/modules/support/schemas.py
  extended (CreateReplyRequest, ReplyRead, ReplyThreadPage, TicketDetailRead)
  against openapi v3/api_design v3/db_design v3/entity_model v3. ruff/mypy
  --strict clean; all 13 tests in test_support_schemas.py pass.

  IMPLEMENTATION T2 (data-layer-builder, PASS): app/modules/support/models.py
  (TicketReply, Ticket.first_response_at, Attachment.ticket_reply_id),
  repository.py (TicketRepository.update, AttachmentRepository.bind_to_reply,
  new TicketReplyRepository), cache.py (TicketReplyRateLimitCache),
  app/core/cache_keys.py (ticket_reply_rate_key) against db_design v3/
  entity_model v3. ruff/mypy --strict clean on all four files. migrations/
  env.py's existing wholesale support-module import confirmed to already
  cover TicketReply (zero-diff registration).

  IMPLEMENTATION T3 (migration-manager, BLOCKED): migrations/versions/
  9132a68b73c8_add_ticket_replies.py written, guarded, and proven via a real
  upgrade->downgrade->upgrade cycle - the migration DDL itself is correct.
  DR-2 resolved (plain CREATE INDEX, not CONCURRENTLY). BLOCKED on a
  deployment-level finding, not a migration defect: the configured database
  role (postgres) is a PostgreSQL superuser (rolsuper=True,
  rolbypassrls=True), which unconditionally bypasses row security regardless
  of FORCE ROW LEVEL SECURITY - db_design v3/entity_model v3's RLS reasoning
  never considered this. Proven via the story's own integration tests
  (2 of 3 direct RLS tests FAIL against this role) and via a scratch
  NOSUPERUSER role (created, exercised, dropped this session) proving the
  CREATE POLICY predicates are themselves correct. See
  docs/catalog/US-4.2-pipeline-status.md v1 for full detail. Holds at
  IMPLEMENTATION pending a human decision. CORRECTION (recorded at the
  HUMAN_REDIRECTED transition below): the BLOCKED note's own claim that "no
  loop_back key under IMPLEMENTATION reaches ... ARCHITECTURE_PLANNING" was
  wrong - stage-map.yaml IMPLEMENTATION.loop_back.blocked_by_architecture
  mechanically reaches it. Only the DB_DESIGN branch has no loop_back key.

  HUMAN_REDIRECTED (2026-09-05T18:30:00Z, decided_by: sbruhov@gmail.com): user
  chose to route via the existing blocked_by_architecture loop_back key
  (stage-map.yaml IMPLEMENTATION.loop_back.blocked_by_architecture) rather
  than the DB_DESIGN branch, over the DR-1(v2)-adjacent alternative of
  revising db_design v3/entity_model v3's RLS claim in place. Direction:
  provision a dedicated non-superuser runtime role (config/session/
  provisioning changes) so TR-AC3's RLS guarantee holds as designed; the two
  currently-failing direct-RLS integration tests
  (test_internal_reply_hidden_from_customer_context_by_rls_alone,
  test_no_actor_kind_set_defaults_to_hiding_internal_reply) are expected to
  pass unchanged once the deployment role changes - implementation_plan v1
  Architectural Change #1 already owns the RLS mechanism decision, and
  provisioning the new role is itself architectural. Recorded as
  HUMAN_REDIRECTED (not a skill-returned CHANGES_REQUIRED[blocked_by_
  architecture], since migration-manager itself returned BLOCKED with no
  key), mirroring the DESIGN_REVIEW->CLARIFICATION HUMAN_REDIRECTED precedent
  at 2026-09-05T09:00:00Z. Routed IMPLEMENTATION -> ARCHITECTURE_PLANNING
  (attempt 2 - ARCHITECTURE_PLANNING v1/PASS already ran for this story at
  2026-09-05T13:00-14:00, producing implementation_plan v1). T1
  (schema-builder) and T2 (data-layer-builder) remain PASS and undisturbed;
  T3's migration file stays on disk pending re-verification once the RLS
  mechanism decision is revised. See docs/catalog/US-4.2-pipeline-status.md
  v2 for the corrected record.

  ARCHITECTURE_PLANNING (attempt 2, PASS): planner revised
  docs/plans/US-4.2-implementation-plan.md to v2 (supersedes v1), adding
  Architectural Change #12 - a dedicated non-superuser app_runtime PostgreSQL
  role, provisioned by a new scripts/db/provision_runtime_role.sql (not an
  Alembic migration), with app/main.py's lifespan switched to a new
  Settings.runtime_database_url for the application's own request-serving
  engine while migrations/env.py keeps using the existing database_url
  (owner role) unchanged. tests/conftest.py's _database fixture must run the
  same provisioning against the ephemeral testcontainers Postgres and rewire
  app.state.db_engine/db_session_factory to the app_runtime-credentialed
  engine, so the two already-written direct-RLS integration tests
  (test_internal_reply_hidden_from_customer_context_by_rls_alone,
  test_no_actor_kind_set_defaults_to_hiding_internal_reply) can actually
  exercise a non-superuser connection. T3's migration file
  (9132a68b73c8_add_ticket_replies.py) and its proven upgrade/downgrade/
  upgrade cycle need no re-run - only re-verification via re-running the two
  RLS tests once the fixture change lands. No blocking issues; three
  non-blocking findings carried into IMPLEMENTATION_PLANNING (file footprint
  wider than app/modules/support/, tests/conftest.py ownership unassigned
  among the four builder skills, provisioning-script documentation deferred
  to documentation-and-adrs). Advanced to IMPLEMENTATION_PLANNING.

  IMPLEMENTATION_PLANNING (v2, PASS): implementation-planner found task_breakdown
  v1 stale (recorded implementation_plan v1 as input; disk has v2) and
  regenerated it as v2 against implementation_plan v2/impact_analysis v2/
  specification v6/db_design v3/entity_model v3/api_design v3/openapi v3. Added
  T4 (NEW) - migration-manager provisions scripts/db/provision_runtime_role.sql
  and wires tests/conftest.py's _database fixture to serve requests through the
  new app_runtime role, then re-verifies T3's two direct-RLS integration tests
  against it - and folded app/main.py's one-line lifespan swap into T5
  (service-and-router-builder), following this story's own v1 precedent of
  bundling adjacent app/core/config.py settings into the service task. Both
  assignments are wider than those skills' usual footprint; implementation_plan
  v2 itself deferred the assignment to this stage rather than naming an owning
  skill, so no loop-back to ARCHITECTURE_PLANNING was used - flagged as a
  non-blocking finding for plan-reviewer/HUMAN_PLAN_APPROVAL instead. T1/T2/T3
  rows carried forward unchanged (already PASS / DDL already proven, no
  re-run). No blocking issues. Advanced to PLAN_REVIEW.

  PLAN_REVIEW (v2, PASS): plan-reviewer re-reviewed implementation_plan v2 and
  task_breakdown v2 (v1 of both, and v1 of this review, are now SUPERSEDED)
  against impact_analysis v2, specification v6, api_design v3/openapi v3, and
  db_design v3/entity_model v3. Full impact-analysis coverage carried forward
  unchanged from v1's already-passed review; Architectural Change #12's four
  new/changed files (scripts/db/provision_runtime_role.sql, app/main.py,
  tests/conftest.py, app/core/config.py's runtime_database_url) have no
  impact_analysis origin but are traced to the recorded HUMAN_REDIRECTED
  transition and TR-AC3 itself, not treated as untraced scope creep. Task
  breakdown T4's layering position (after T3, gating T7) is correct even
  though it sits outside the router/service/repository chain. Risks 7-8 and
  the full-suite regression addition judged concrete, not generic. Two
  non-blocking findings recorded (impact_analysis staleness on Change #12's
  files; T4/T5 skill-assignment judged acceptable) - neither blocks, neither
  requires a loop-back. No Critical/Major findings. docs/reviews/plans/
  US-4.2-plan-review.md v2. Advanced to HUMAN_PLAN_APPROVAL - awaiting
  /so:approve or /so:reject.

  HUMAN_PLAN_APPROVAL (attempt 2): human approved via /so:approve at
  2026-09-05T21:15:00Z (decided_by: sbruhov@gmail.com), no comment. Required
  artifacts implementation_plan v2, task_breakdown v2, plan_review v2 all
  current (status DRAFT, not SUPERSEDED/ARCHIVED) with recorded automated
  verdict PASS and no blocking findings. Advanced to TEST_WRITING.

  TEST_WRITING (re-entry, PASS): test-writer found its own v1 outputs
  (test_strategy, ac_test_matrix, test_generation_report) stale against
  implementation_plan v2/task_breakdown v2/plan_review v2 (recorded
  provenance was v1/v1/v1) and re-verified rather than regenerated: grep
  count of test functions in the three story-owned files (60 integration /
  42 unit-service / 12 unit-schemas) matches v1's reported figures exactly,
  confirming zero drift, consistent with implementation_plan v2's own
  Testing Strategy section ("unaffected by v2" / "no test rewrite needed"
  for Architectural Change #12's runtime-role change). Bumped all three
  artifacts to v2 for provenance only, with a Revision Note each. No
  application or test code changed. PASS, no blocking issues. Advanced to
  IMPLEMENTATION (re-entry - T1/T2 already PASS, T3 DDL already proven;
  T4 (migration-manager: scripts/db/provision_runtime_role.sql +
  tests/conftest.py) is next per task_breakdown v2, docs/catalog/
  US-4.2-pipeline-status.md v2).

  IMPLEMENTATION T4 (migration-manager, BLOCKED): scripts/db/provision_runtime_role.sql
  written (idempotent CREATE ROLE app_runtime + dynamic GRANT/ALTER DEFAULT
  PRIVILEGES via current_database(), no hard-coded db/role name) and
  tests/conftest.py's _database fixture rewired to provision the role once per
  session then serve all test requests (db_session, client, cleanup_users)
  through an app_runtime-credentialed engine, matching implementation_plan v2
  Architectural Change #12/app/main.py's intended real-deployment wiring.
  Verified: idempotency (ran twice, zero errors); GRANT completeness/Risk 7
  (full non-support integration suite, 261 tests, plus 26 of support's own
  non-reply tests, pass unchanged against app_runtime). BLOCKED on the two
  named re-verification tests plus a third previously-masked-passing one -
  not a T4 defect: test_support_router.py's _seed_reply() helper
  (test-writer/TEST_WRITING-owned, outside T4's scope) never sets
  app.actor_kind before seeding internal-visibility replies, which
  ticket_replies_write's WITH CHECK now correctly rejects under genuine RLS
  enforcement (previously masked by the superuser bypass). No
  IMPLEMENTATION.loop_back key fits a test-file fix, mirroring T3's own
  BLOCKED precedent above. Full detail and the exact fix needed:
  docs/catalog/US-4.2-pipeline-status.md v3, T4 section. Holds at
  IMPLEMENTATION pending a human decision on how to route (likely
  HUMAN_REDIRECTED to TEST_WRITING, mirroring the T3->ARCHITECTURE_PLANNING
  precedent, then re-entering IMPLEMENTATION at T4 re-verification).

  HUMAN_REDIRECTED (2026-09-05T22:30:00Z, decided_by: sbruhov@gmail.com):
  resolves migration-manager's T4 BLOCKED verdict above. User directed routing
  IMPLEMENTATION -> TEST_WRITING (no IMPLEMENTATION.loop_back key fits a
  test-file fix, mirroring the T3->ARCHITECTURE_PLANNING precedent at
  2026-09-05T18:30:00Z) so test-writer can add
  `await db_session.execute(text("SET LOCAL app.actor_kind = 'agent'"))`
  before each internal-visibility `_seed_reply()` call in
  test_internal_reply_hidden_from_customer_context_by_rls_alone,
  test_no_actor_kind_set_defaults_to_hiding_internal_reply, and
  test_agent_context_sees_internal_reply_via_rls, then re-enter IMPLEMENTATION
  at T4 re-verification once those tests pass. attempt := 2 (TEST_WRITING's
  prior recorded attempt was 1, per history.jsonl 2026-09-05T21:30:00Z).

  TEST_WRITING (HUMAN_REDIRECTED re-entry, attempt 2, PASS): fixed exactly
  the three named tests in tests/integration/modules/support/test_support_
  router.py per the user's direction - each now sets `SET LOCAL
  app.actor_kind = 'agent'` before seeding its internal-visibility reply;
  test_no_actor_kind_set_defaults_to_hiding_internal_reply additionally runs
  `RESET app.actor_kind` immediately after seeding so the GUC does not
  persist into its Act phase (SET LOCAL lasts for the rest of the
  transaction, and this fixture's test body is one savepoint-based
  transaction never rolled back mid-test - verified empirically, not
  assumed). No application code, no other test, no other file changed.
  Verified: the three tests pass individually and together; full support
  suite 35 failed/29 passed (was 38/26 - exactly the three RLS tests moved
  to passing, no other regression, remaining 35 are the expected pre-T5/T6
  red state); full non-support integration suite 261/261 passed, unchanged;
  ruff/mypy --strict clean on the changed file. test_strategy/ac_test_matrix/
  test_generation_report bumped to v3 for provenance (no AC/row changes).
  No blocking issues. Advanced to IMPLEMENTATION, implementation_substep :=
  service-and-router-builder (T4 is now fully resolved - re-verification
  proven; T5 next per task_breakdown v2).

  IMPLEMENTATION T5/T6 (service-and-router-builder, BLOCKED): TicketReplyService
  (create_reply/get_ticket_detail), module-owned exceptions
  (TicketNotFoundError/InsufficientPermissionError/TicketClosedError/
  TicketReplyRateLimitError), EmailSender's two new methods, config/.env.example
  additions, app/main.py's runtime_database_url line, get_rls_session/
  resolve_actor_kind/TicketReplyServiceDep, and both new routes
  (POST .../replies, GET /support/tickets/{id}) all written and independently
  verified: ruff/mypy --strict/lint-imports clean; OpenAPI render matches
  openapi v3 exactly (path parameter renamed to {id} to match the contract
  literally); 61/61 support unit tests, 63/64 support integration tests,
  261/261 non-support integration tests pass; 93.90% combined coverage on
  app/modules/support/ (service.py 95%, router.py 100%). BLOCKED on two
  TEST_WRITING-owned test defects, not a T5/T6 code defect - see
  docs/catalog/US-4.2-pipeline-status.md v5 T5/T6 section and this file's
  blocking_issues above for full detail. Recommended resolution mirrors T4's
  own HUMAN_REDIRECTED precedent exactly: route to TEST_WRITING to fix the
  two named tests, then re-enter IMPLEMENTATION at T5/T6 re-verification.

  HUMAN_REDIRECTED (2026-09-05T23:45:00Z, decided_by: sbruhov@gmail.com):
  resolves service-and-router-builder's T5/T6 BLOCKED verdict above. User
  directed routing IMPLEMENTATION -> TEST_WRITING (no IMPLEMENTATION.loop_back
  key fits a test-file fix, mirroring the T3->ARCHITECTURE_PLANNING and
  T4->TEST_WRITING precedents) with two specific fixes: (1) add
  `SET LOCAL app.actor_kind = 'agent'` before the internal-visibility
  `_seed_reply()` call in
  test_get_ticket_detail_hides_internal_reply_from_customer_but_shows_to_agent;
  (2) add an optional `created_at` parameter to `_seed_reply()` and update
  test_get_ticket_detail_paginates_reply_thread_oldest_first to pass distinct
  sequential `created_at` values for its three seeded replies. attempt := 3
  (TEST_WRITING's prior recorded attempt was 2, per history.jsonl
  2026-09-05T22:45:00Z).

  TEST_WRITING (HUMAN_REDIRECTED re-entry, attempt 3, PASS): fixed exactly
  the two named tests in tests/integration/modules/support/test_support_
  router.py per the user's direction - (1) added SET LOCAL app.actor_kind =
  'agent' before the internal-visibility _seed_reply() call in
  test_get_ticket_detail_hides_internal_reply_from_customer_but_shows_to_agent
  (no RESET needed - its Act phase runs through real HTTP requests, and
  get_rls_session sets its own fresh app.actor_kind per request from the
  caller's own token); (2) added an optional created_at parameter to
  _seed_reply() and updated test_get_ticket_detail_paginates_reply_thread_
  oldest_first to seed three replies with base_time + timedelta(seconds=i),
  removing the tie on PostgreSQL's transaction-frozen now(). No application
  code, no other test changed. Verified: both tests pass individually and
  together, deterministic across 3 repeated runs; full support suite
  (unit+integration) 125/125 passed, 93.90% coverage; full repository suite
  688/688 passed; ruff/ruff format/mypy --strict/lint-imports all clean.
  test_strategy/ac_test_matrix/test_generation_report left at v3 unchanged -
  no upstream design artifact changed, only test code fixed a discovered
  bug, tracked via git rather than a document version bump. T5 and T6 (see
  docs/catalog/US-4.2-pipeline-status.md v6) are both now PASS -
  IMPLEMENTATION's four sub-steps are all complete. Advanced to QUALITY_GATE.

  QUALITY_GATE (PASS): gate-enforcer ran the full Definition-of-Done gate
  fresh. pre-commit run --all-files clean (7/7 hooks; detect-secrets' first
  run flagged only a pre-existing unstaged .secrets.baseline, staged with no
  content change, re-run clean); mypy app tests clean (146 files); lint-imports
  clean (6/6 contracts kept, zero pyproject.toml diff against HEAD - no config
  drift); full repository suite 688/688 passed, 96.30% coverage (support/
  service.py 95%, support/router.py 100%); migration upgrade->downgrade->
  upgrade re-proven fresh (9132a68b73c8). Runtime rules: ORM containment clean
  (no model/AsyncSession import in router.py, service.py returns only *Read
  types); eager loading N/A (no relationship() anywhere in support/models.py -
  documented module precedent); cache TTL present on every write
  (TicketReplyRateLimitCache uses expire()); cross-module discipline clean (no
  other-module router import in service.py); no banned idioms (Any/type:
  ignore/cast/os.getenv-os.environ) found in app/; all four routes declare
  response_model+status_code, both inbound schemas set extra="forbid", all
  three *Read schemas declare from_attributes=True with explicit field lists,
  .env.example updated (SUPPORT_QUEUE_EMAIL, RUNTIME_DATABASE_URL), OpenAPI
  renders (28 paths). No blocking issues. docs/evidence/
  US-4.2-implementation-report.md v1, docs/evidence/US-4.2-quality-gate-
  report.md v1. Advanced to IMPLEMENTATION_VERIFICATION.

  IMPLEMENTATION_VERIFICATION (PASS): implementation-verifier independently
  re-verified AGENTS.md 6.5/6.6/6.7 and the 5 security-case matrix by reading
  the migration file and every touched app/modules/support/ source file
  directly, not inferred from gate-enforcer's summary. 6.5: both add_column
  calls, the create_foreign_key call, and both CREATE POLICY statements in
  9132a68b73c8_add_ticket_replies.py are each individually guarded via
  sa.inspect(op.get_bind())/pg_policies; downgrade() performs real inverse
  operations for every object created. 6.6: ORM containment clean
  (create_reply/get_ticket_detail return ReplyRead/TicketDetailRead only,
  router.py has no models/AsyncSession import); eager loading N/A (no
  relationship() in models.py, confirmed by direct read); cache TTL present
  (TicketReplyRateLimitCache.record_and_check calls expire()); cross-module
  discipline clean (service.py's only cross-module collaborator is
  UserServiceProtocol, injected as the concrete UserService via
  dependencies.py, never a router). 6.7: response_model+status_code on both
  new routes; CreateReplyRequest sets extra="forbid" with no privilege field;
  .env.example diff confirmed (SUPPORT_QUEUE_EMAIL, RUNTIME_DATABASE_URL); no
  sensitive field in ReplyRead/TicketDetailRead. 5: both new routes
  (POST .../replies, GET .../{id}) have no-token/expired/malformed/revoked
  tests plus a cited insufficient-permissions case (visibility=internal 403
  for POST; tickets:read-lacking-agent 404 for GET) - the ownership-404 branch
  is a separately documented design decision (API_DESIGN Open Question #2),
  not a gap. No Critical/Major findings. docs/verification/
  US-4.2-implementation-verification.md v1. Advanced to SECURITY_REVIEW.

  SECURITY_REVIEW (PASS): security-reviewer audited every file this story
  touches or adds against AGENTS.md section 7's six non-negotiable rules.
  Password storage/reversible-encryption rows: N/A (no credential field
  anywhere in this story). No token/hash/PII in logs: Pass (only static log
  messages; the one interpolated value, support_queue_email in
  send_ticket_reply_queue_notification, is a configured internal address,
  not a token/hash/PII field). extra="forbid" + privilege exclusion: Pass
  (CreateReplyRequest excludes id/ticket_id/author_id/created_at).
  Parameterized SQL only: Pass (all ORM queries bound; get_rls_session's two
  text() calls bind actor_kind/actor_id; migration's op.execute() calls are
  compile-time constants; provision_runtime_role.sql's one dynamic value
  uses PostgreSQL's %I-quoting format() against current_database(), not
  string interpolation of user input). Uniform auth-failure response: Pass
  (TicketNotFoundError returns the same 404 for unknown/not-owned/wrong-
  actor-kind, never confirming ticket existence to an unauthorized caller).
  No Critical or Major findings. Two Low advisory findings carried/recorded
  (US-4.1's pre-existing sequentially-guessable ticket_number; the new
  scripts/db/provision_runtime_role.sql's placeholder role password, which
  matches the same must-override-in-deployment discipline already used for
  Settings.jwt_secret_key/mfa_secret_encryption_key). docs/reviews/security/
  US-4.2-security-review.md v1. Advanced to RECONCILIATION.

  RECONCILIATION (PASS): reconciliation-reviewer confirmed every AC (TR-AC1-
  TR-AC7) has a matrix row in docs/tests/US-4.2-ac-test-matrix.md v3, that
  every named test function exists at its stated path (grep count 60/42/12
  matches the matrix's recorded totals), and read each test's assertions to
  confirm they check the AC's actual stated behavior rather than mere
  proximity - spot-checked directly: the two OD-5/OD-8 resolved-ticket tests
  (agent reply keeps status "resolved"; customer reply moves it to
  "waiting_on_support" - the correct divergent outcomes, not a copy-paste),
  the three RLS tests (one deliberately bypasses the repository/service with
  a raw SELECT through a customer-context connection, per the NFR's own
  "application filter deliberately disabled" requirement), the four
  attachment-binding/BR-016-indistinguishability tests, the full FR-4
  401/404 auth-failure matrix, and FR-7's validation boundaries. Compared
  shipped app/modules/support/service.py::create_reply's status/
  first_response_at table (lines 504-518) and exceptions.py's five
  type_slugs against the approved spec directly: no drift found. The two
  gaps already recorded in ac-test-matrix v3's own "Gaps Not Covered"
  section (API_DESIGN OQ-2's exact scope-split combination, not reachable
  under the shipped role seed; migration reversibility, owned by
  migration-manager) are pre-existing documented limitations, not
  reconciliation defects. No blocking issues. docs/reviews/reconciliation/
  US-4.2-reconciliation.md v1, docs/reconciliation/US-4.2-traceability.md
  v1. Advanced to HUMAN_PR_APPROVAL - awaiting /so:approve or /so:reject.

  HUMAN_PR_APPROVAL: human approved via /so:approve at 2026-09-06T02:00:00Z
  (decided_by: sbruhov@gmail.com), no comment. Required artifacts
  implementation_verification v1, security_review v1, reconciliation v1,
  traceability v1 all current (status DRAFT, not SUPERSEDED/ARCHIVED) with
  recorded automated verdict PASS and no blocking findings. Advanced to
  PR_PREPARATION.

  PR_PREPARATION (PASS): pr-preparer confirmed all four required upstream
  verdicts directly from their reports (gate-enforcer PASS via
  docs/evidence/US-4.2-quality-gate-report.md v1; implementation-verifier PASS
  v1; security-reviewer PASS v1; reconciliation-reviewer PASS v1), drafted the
  PR title ("feat: ticket replies with RLS-enforced internal visibility
  (US-4.2)"), summary, and a test-plan checklist built from
  docs/reconciliation/US-4.2-traceability.md v1 covering all seven ACs plus
  the quality-gate suite/coverage/migration-cycle results. Confirmed
  .env.example current (SUPPORT_QUEUE_EMAIL, RUNTIME_DATABASE_URL both
  present). Commit-hygiene review traced every changed file to this story's
  implementation_plan v2 (including Architectural Change #12's wider
  footprint) except one: .gitignore's docs/knowledge addition has no
  traceable origin in this story's artifacts - recorded as a non-blocking
  finding for the human to confirm before merge, not a gate failure. No
  blocking issues. docs/pr/US-4.2-pr-summary.md v1. Advanced to
  READY_FOR_PR - awaiting /so:approve or /so:reject.

  READY_FOR_PR: human approved via /so:approve at 2026-09-06T02:30:00Z
  (decided_by: sbruhov@gmail.com), comment: "Leaving docs/knowledge in
  .gitignore as acknowledged. PR summary approved." Required artifact
  pr_summary v1 confirmed current (status DRAFT, not SUPERSEDED/ARCHIVED)
  with recorded automated verdict PASS and no blocking findings; the
  .gitignore/docs/knowledge non-blocking finding is explicitly acknowledged
  by this approval, not a gate defect. Per AGENTS.md section 1, this gate
  records human sign-off only - no Pull Request was created, pushed, or
  merged by the harness. Advanced to COMPLETED - a human must still confirm
  the Pull Request is merged before /so:approve can advance to ARCHIVED.

  COMPLETED (/so:next, continue-flow step 3): pending_human_gate was still
  null on entry to this stage (the prior READY_FOR_PR->COMPLETED transition
  recorded the advance but did not populate the gate). Built it now per
  state-schema.md - required artifact pr_summary v1 (docs/pr/US-4.2-pr-
  summary.md) confirmed current, status DRAFT; automated_verdict PASS
  carried from PR_PREPARATION (no automated stage runs between READY_FOR_PR
  and COMPLETED). No skill invoked, no stage transition - status set to
  WAITING_FOR_HUMAN. Awaiting /so:approve (confirming the PR is merged) or
  /so:reject.

  COMPLETED (RETRACTED, 2026-09-06T03:30:00Z, decided_by: sbruhov@gmail.com):
  the /so:approve recorded above at 2026-09-06T03:15:00Z advanced current_stage
  to ARCHIVED on the human's stated confirmation that the Pull Request was
  merged. That claim was checked against the actual repository state
  (git log/branch -a, gh pr list) and found false: no feat/us-4.2-* branch or
  Pull Request exists on the remote, and origin/main's tip (9948e96) is still
  US-4.1's archive commit. The COMPLETED gate's `meaning` in stage-map.yaml is
  "A human confirms the Pull Request has been merged" - that precondition does
  not hold, so the approval is invalid. Reverted current_stage to COMPLETED,
  status to WAITING_FOR_HUMAN, pending_human_gate rebuilt as PENDING
  (unchanged from its original READY_FOR_PR->COMPLETED population), archived_at
  cleared. The corresponding history.jsonl HUMAN_APPROVED event is left
  in place per the append-only rule; this event supersedes it. Awaiting an
  actual merged PR before /so:approve can validly re-run on this gate.

  COMPLETED (/so:approve, 2026-09-06T04:00:00Z, decided_by: sbruhov@gmail.com,
  no comment): required artifact pr_summary v1 (docs/pr/US-4.2-pr-summary.md)
  reconfirmed current (status DRAFT, not SUPERSEDED/ARCHIVED), automated_verdict
  PASS, no blocking findings. Per the COMPLETED gate's approval_precondition,
  the merge claim was independently verified against the actual repository
  (not taken on the approver's word): `git fetch origin` then
  `git merge-base --is-ancestor 34a0e7d origin/main` returned true, and
  `gh pr view 17 --json state,mergedAt,baseRefName,headRefName` confirmed
  state=MERGED, mergedAt=2026-09-05T00:16:27Z, base=main,
  head=feat/us-4.2-ticket-replies. origin/main's tip (44587b7) is that PR's
  merge commit. This supersedes the 03:30 RETRACTED entry above - the
  underlying condition it flagged (no merged PR found) no longer holds.
  Advanced current_stage COMPLETED -> ARCHIVED per stage-map.yaml on_approve;
  pending_human_gate cleared; status set to ARCHIVED. No Pull Request was
  created, pushed, or merged by the harness in this step - PR #17 was already
  merged prior to this approval. Archive-mode consolidation (delivery_summary,
  project_state) is /so:archive's responsibility, not yet run.

  ARCHIVED (/so:archive, 2026-09-06T05:00:00Z): re-verified PR #17's merge
  independently (git fetch + merge-base --is-ancestor + gh pr view - same
  result as the 04:00 approval). archived_at moved from 04:00 (the mechanical
  COMPLETED->ARCHIVED transition per stage-map.yaml on_approve) to 05:00 (this
  consolidation run actually completing), mirroring US-4.1's precedent where
  the two coincided in one session. Note also: archive-flow.md's own stated
  precondition ("current_stage == COMPLETED") was already stale on entry to
  this run - current_stage was already ARCHIVED per the 04:00 mechanical
  transition above, exactly the same two-step pattern US-4.1's history.jsonl
  (254-255) recorded; treated as the document being stale, not the state
  being wrong. Wrote docs/evidence/US-4.2-delivery-summary.md (v1); set
  docs/catalog/stories.yaml US-4.2 state to ARCHIVED (delivered_by.pr: 17);
  backfilled 22 US-4.2 story-level artifacts' front-matter status from DRAFT
  straight to ARCHIVED (story and story_catalog carry no front matter;
  delivery_summary was created ARCHIVED directly, never DRAFT; pipeline_status
  stays DRAFT, preserved as a historical cross-story artifact) - no skill this
  story ever wrote APPROVED, same gap US-4.1's archive run flagged; updated
  docs/knowledge/project-state.md (Delivered Capabilities row, two new Known
  Constraints for the app_runtime role and BR-017's unbuilt auto-close half,
  Archived Stories row) - applied directly per archive-flow step 5. Proposed
  (NOT applied) one docs/product/business-rules.md entry (ticket reply status
  transitions) and one docs/ARCHITECTURE.md entry (dedicated non-superuser
  app_runtime runtime-DB-role pattern) for human review. No GitHub sync
  (source local_only). Cleared active-story.yaml (active_story: null,
  status: ARCHIVED). Five pre-existing uncommitted working-tree edits
  (AGENTS.md, docs/workflow/stage-map.yaml, .claude/commands/so/approve.md,
  plus this file and history.jsonl) are outside this skill's ownership and
  were left untouched. Known limitations (API_DESIGN OQ-2's untested scope
  combination, BR-017's unbuilt auto-close half, app_runtime provisioning
  requirement, ticket_number guessability) all carried in the delivery
  summary. Recommends /so:start US-4.3 next (sole remaining EPIC-4 BACKLOG
  story).
