# Execution state of the active delivery workflow.
# Schema: docs/workflow/state-schema.md
# Written by story-orchestrator ONLY. Stage skills return a Result Envelope;
# the orchestrator records the transition here and appends one event to
# docs/workflow/history.jsonl.

version: 1

story: US-5.3
workflow: story-delivery

current_stage: PR_CREATION
previous_stage: PR_PREPARATION
last_completed_stage: PR_PREPARATION

status: IN_PROGRESS
attempt: 1

last_invoked_skill: pr-preparer

last_result:
  verdict: PASS
  stage: PR_PREPARATION
  recorded_at: "2026-09-12T21:32:00Z"

last_artifacts:
  - "docs/pr/US-5.3-pr-summary.md"

implementation_substep: null

pending_human_gate: null

note: >
  Plan v2 incorporates the human's binding OD-1..OD-6 resolutions
  (2026-09-08T21:00:00Z rejection comment) as firm architecture, superseding
  v1. docs/decisions/US-5.3-open-decisions.md (v1, DRAFT) and
  docs/specifications/US-5.3-spec.md FR-12 prose are now stale relative to
  these resolutions but remain unedited (out of story-orchestrator's
  ownership) - downstream stages should treat the plan's resolutions as
  authoritative until us-clarifier/story-spec-writer formally update their
  own artifacts.

blocking_issues: []

non_blocking_findings:
  - "OD-1 (High): FR-12/TK-AC12's 429 Retry-After handling is unimplementable until it's decided how the header reaches ApiError (httpClient.ts does not pass response.headers through). Needs resolution before implementation."
  - "OD-2, OD-3, OD-4, OD-5, OD-6 (Medium/Low) remain open, carried forward as Open Questions 2-6 in the specification."
  - "Page size (limit) for GET /support/tickets is unstated in both story and spec (Low)."
  - "Story front-matter issue_number: TODO is stale (active-story.yaml already records issue_number: 26) - a backlog-sync-owned correction."
  - "API_DESIGN: NOT_APPLICABLE - US-5.3 is frontend-only, all endpoints already contracted in US-4.1-US-4.4 OpenAPI designs; OD-1 assessed as a frontend httpClient concern, not a backend API contract change."
  - "IMPACT_ANALYSIS: OD-1 determines whether errorNormalization.ts/apiErrorHelpers.ts are in scope at all (not just how they change); httpClient.ts is affected regardless via the unconditional Idempotency-Key header extension."
  - "IMPACT_ANALYSIS: renderWithProviders test-utils has no precedent for a screen reading a URL param (TicketDetailScreen is the first) - workable, flagged for IMPLEMENTATION."
  - "IMPACT_ANALYSIS: no styling layer exists anywhere in frontend/src today; pre-existing gap, not introduced by this story."
  - "ARCHITECTURE_PLANNING: OD-1 bounds FR-12/TK-AC12's Retry-After implementation to a 429-only additive httpClient.ts helper (getRetryAfterSeconds()); no other FR depends on OD-1's resolution."
  - "ARCHITECTURE_PLANNING: OD-2/3/4/5/6 remain OPEN; plan is designed to proceed regardless of how each eventually resolves."
  - "IMPLEMENTATION_PLANNING: OD-1 bounds T1's Retry-After scope to the 429-only reading; if OD-1 resolves toward a broader mechanism, only T1 needs revisiting."
  - "PLAN_REVIEW (v1, superseded): two Low-severity informational notes only, resolved by the v2 replan."
  - "ARCHITECTURE_PLANNING (v2): OD-1..OD-6 now firm architecture per human resolution, not workarounds. Prior 'OD-1 blocking dependency' risk retired. OD-1's ApiError.retryAfterSeconds is now first-class, not FR-12-scoped."
  - "ARCHITECTURE_PLANNING (v2): docs/decisions/US-5.3-open-decisions.md (v1, DRAFT) and spec FR-12 prose are stale relative to the human's resolutions but unedited (not story-orchestrator's artifact) - plan v2's resolutions are authoritative for downstream stages until formally reconciled."
  - "IMPLEMENTATION_PLANNING (v2): task_breakdown v2 sequences against plan v2's OD-1..OD-6 resolutions; same reconciliation debt on open-decisions.md/spec FR-12 carried forward, flagged for PLAN_REVIEW as known debt not invented scope."
  - "PLAN_REVIEW (v2): all six OD resolutions verified firmly incorporated (not hedged) in plan/task_breakdown v2 - the specific defect that caused v1's rejection. Two pre-existing Low-severity notes only (useRetryAfterCountdown.ts hook, apiErrorHelpers.test.ts), both correctly traceable."
  - "TEST_WRITING: docs/decisions/US-5.3-open-decisions.md (v1, DRAFT) and spec FR-12 prose remain stale relative to implementation_plan/task_breakdown v2's binding OD-1..OD-6 resolutions; neither is in this stage's input registry, treated as stale not blocking, reconciliation debt carried forward."
  - "TEST_WRITING: offeredActionsForStatus must be a named export from TicketDetailScreen.tsx (test-strategy item 10) - implementation_plan v2 does not state export visibility; TicketDetailScreen.test.tsx's TK-AC9 coverage depends on it. Flagged for IMPLEMENTATION."
  - "TEST_WRITING: three AppRoutes.test.tsx cases plus one each in GuestOnlyRoute.test.tsx/LoginScreen.test.tsx/MfaVerifyScreen.test.tsx were renamed and retargeted from '/' to '/tickets' (FR-15) - a rename, not deleted US-5.1/US-5.2 coverage."
  - "TEST_WRITING: tests are in expected TDD-red state (module resolution fails until IMPLEMENTATION T1-T19 land); no suite run, no coverage claimed. Self-checks recorded in test_generation_report: 94/94 matrix references verified, 280/280 test titles pattern-compliant with no duplicates, 0 vi.mock() calls, crypto.randomUUID() confirmed working under jsdom."
  - "IMPLEMENTATION (frontend-builder, attempt 1): all T1-T19 implementation code complete, type-check clean, 289/291 tests pass. CHANGES_REQUIRED -> loop_back TEST_WRITING (key changes_required_tests) for 3 confirmed test-file defects (not implementation defects, each reproduced in isolation): (1) useRetryAfterCountdown.test.ts's 'clears_its_interval_on_unmount' spies on globalThis.clearInterval without restoring it, breaking the next test in file order; (2) NewTicketScreen.test.tsx's over-length-field test uses user.type() on 151/5001/51-char strings which hangs (confirmed independent of app code) - needs fireEvent.change or user.paste instead; (3) useTickets.test.ts line 135/142 triggers a genuine ESLint prefer-const false positive whose own autofix would break at runtime - needs the let+closure+reassign pattern reworked."
  - "IMPLEMENTATION (frontend-builder, attempt 1): separate finding needing human/orchestrator sign-off, NOT a TEST_WRITING item - TicketDetailScreen.tsx's mandated dual export (TicketDetailScreen + offeredActionsForStatus, required by TK-AC9) triggers react-refresh/only-export-components with no code-side fix; frontend-builder proposes a frontend/.eslintrc.cjs override for src/screens/TicketDetailScreen.tsx mirroring the existing src/store/**/*.tsx exception, but did not apply it (enforcement-config edits are not a skill's to make unilaterally)."
  - "IMPLEMENTATION (frontend-builder, attempt 1): three plan-deviations from implementation_plan v2's literal 'invalidates' wording, all defended with isolated-repro evidence - useCloseTicket/useReopenTicket/useReplyToTicket write the mutation response into cache via setQueryData then invalidate with refetchType:'none' (except useReplyToTicket's detail-key invalidation, left as a normal fetch-triggering invalidation per FR-6's need to pick up a waiting_on_customer->waiting_on_support transition); useTicketDetail's two sibling queries set notifyOnChangeProps:'all' to work around a confirmed TanStack Query v5 tracked-properties notification gap. Flagged for IMPLEMENTATION_VERIFICATION/RECONCILIATION to confirm these are acceptable readings of Change 5, not drift."
  - "IMPLEMENTATION (frontend-builder, attempt 1): TicketListScreen.tsx's status-filter option labels were invented (not spec-derived) to avoid substring collision with raw ticket.status text in the same DOM; TicketDetailScreen.tsx's 'Created' header field is rendered via a hardcoded Intl.DateTimeFormat('en-US', ...) rather than raw ISO or the profile's locale field, to avoid a same-day timestamp collision with first_response_at/reply timestamps. Both flagged for reviewer awareness/sign-off, not applied as unprompted spec changes."
  - "IMPLEMENTATION (frontend-builder, attempt 1): coverage unmeasured this run - npm run test:coverage does not emit a report while any test fails; re-run once the 3 test-file defects are fixed. supportApi.ts and useTickets.ts (pre-existing untracked files found at dispatch, origin unknown) were verified correct against task_breakdown T4/T6 and the US-4.1-4.4 OpenAPI contracts and kept as-is (prettier formatting only). A genuine reply-ordering bug (new replies appended to the oldest page instead of prepended to the newest) was caught and fixed during self-review."
  - "TEST_WRITING (attempt 2, fix-only pass): all 3 confirmed test-file defects fixed without touching implementation code - useRetryAfterCountdown.test.ts's spy now restored, NewTicketScreen.test.tsx's over-length fields use fireEvent.change instead of user.type(), useTickets.test.ts's let/reassign reworked to a mutable ref object sidestepping the confirmed ESLint prefer-const false positive. 291/291 tests pass (up from 289/291); type-check and format:check clean. npm run lint --max-warnings=0 still exits non-zero repo-wide, but the sole cause is the pre-existing, separately-flagged TicketDetailScreen.tsx react-refresh/only-export-components warning (0 errors) - confirms that item is isolated and needs only the proposed .eslintrc.cjs sign-off, not further test changes. test_generation_report bumped to v2 (Attempt 2 addendum); test_strategy/ac_test_matrix unchanged at v1 (no AC coverage or test-function renames)."
  - "IMPLEMENTATION (frontend-builder, close-out re-dispatch): PASS. 291/291 tests, type-check clean, coverage clears 85% floor on all 4 metrics (statements 97.62%, branches 95.29%, functions 87.39%, lines 97.62%), no new dependency (OD-5 holds). format:check was NOT actually clean on entry despite attempt-2's self-report - 7 files including implementation file httpClient.ts had unformatted content; fixed with a whitespace-only prettier pass, now clean (flag: trust but verify gate self-reports going forward). Lint still exits non-zero on exactly the one pre-flagged, non-blocking TicketDetailScreen.tsx react-refresh/only-export-components warning - QUALITY_GATE's T20 lint&&format&&type-check&&coverage chain will abort at lint until the documented .eslintrc.cjs override gets its separate sign-off; this is expected, not a new regression. Spot-check of ac_test_matrix's highest-risk named test functions against the working tree found no dangling references."
  - "STANDING ITEM for a human/orchestrator decision, not yet resolved: TicketDetailScreen.tsx's mandated offeredActionsForStatus named export (TK-AC9) triggers react-refresh/only-export-components with no code-side fix. Proposed frontend/.eslintrc.cjs addition (mirrors the existing src/store/**/*.tsx exception): { files: [\"src/screens/TicketDetailScreen.tsx\"], rules: { \"react-refresh/only-export-components\": \"off\" } }. Not applied by any stage skill (enforcement-config edits require explicit sign-off, not a skill's to make unilaterally) - this repo-wide `npm run lint --max-warnings=0` failure will keep surfacing at QUALITY_GATE until decided."
  - "QUALITY_GATE (gate-enforcer, attempt 1): CHANGES_REQUIRED, loop_back IMPLEMENTATION. AGENTS.md sections 2/6 require the frontend lint script green with zero findings, no warning/error distinction and no pending-config-change exception, so the standing eslintrc item above is a genuine, mechanically-enforced DoD gate failure, not a soft advisory - gate-enforcer explicitly declined to wave it through despite confirming it's a pre-diagnosed, no-code-fix, single-warning item. Everything else clean: format:check/type-check/test:coverage all pass (291/291 tests, coverage 97.62/95.29/87.39/97.62 vs 85% floor), all runtime-rule/layering checks compliant. gate-enforcer itself flagged that re-looping IMPLEMENTATION without a human decision on the eslintrc override will reproduce this exact failure - this loop cannot self-resolve without that sign-off."
  - "HUMAN SIGN-OFF (2026-09-12): option (a) approved. frontend/.eslintrc.cjs override added for src/screens/TicketDetailScreen.tsx (mirrors the src/store/**/*.tsx exception), scoped to react-refresh/only-export-components only. npm run lint --max-warnings=0 independently confirmed clean (0 errors, 0 warnings)."
  - "IMPLEMENTATION (frontend-builder, attempt 3): re-verification after the eslintrc sign-off. lint/format:check/type-check all clean. test:coverage surfaced a new test-file defect (not present in attempt 1's close-out run): ProfileScreen.test.tsx's test_profile_screen_422_validation_error_maps_errors_array_onto_matching_fields times out at the 5000ms default under full-suite load (reproduced twice), passes standalone in 3.2s - same slow-user.type()-on-long-string pattern (200-char input) already fixed in NewTicketScreen.test.tsx but missed in this file. CHANGES_REQUIRED, loop_back_stage TEST_WRITING (key changes_required_tests)."
  - "TEST_WRITING (attempt 3, fix-only pass): fixed the single confirmed defect in frontend/src/screens/ProfileScreen.test.tsx (US-5.2 file) - test_profile_screen_422_validation_error_maps_errors_array_onto_matching_fields's 200-char user.type() call replaced with fireEvent.change, same pattern as NewTicketScreen.test.tsx's earlier fix. No implementation code touched. Verified twice: 291/291 tests pass both full-suite runs (up from 290/291); coverage 97.47/95.29/87.39/97.47 vs 85% floor; lint --max-warnings=0 clean (0 errors, 0 warnings, confirms the TicketDetailScreen.tsx eslintrc sign-off holds); format:check and type-check clean. test_generation_report bumped to v3 (Attempt 3 addendum); test_strategy/ac_test_matrix unchanged at v1."
  - "IMPLEMENTATION (frontend-builder, attempt 4, close-out): PASS. 291/291 tests, coverage 97.47/95.29/87.39/97.47 vs 85% floor, lint --max-warnings=0 clean (0/0), format:check and type-check clean. Self-checks: 0 bare fetch()/axios in screens/, no new localStorage/sessionStorage token usage. IMPLEMENTATION stage complete."
  - "QUALITY_GATE (gate-enforcer, 2nd dispatch): PASS. All Part A' mechanical checks (lint/format:check/type-check/test:coverage) and Part B' runtime-rule checks (API-boundary containment, session-token handling, store discipline [N/A], banned idioms, contract/security spot-check) re-run fresh and pass or confirmed N/A. The sole attempt-1 failure (lint's TicketDetailScreen.tsx:44 warning) is resolved by the human-approved eslintrc override, re-verified by direct command output. implementation_report bumped to v2, quality_gate_report to v2. No bypass used or proposed at either dispatch."
  - "IMPLEMENTATION_VERIFICATION (implementation-verifier, attempt 1): PASS. Frontend-track §6.6/§6.7 items independently re-derived (not trusted from reports) with file:line evidence; the two self-flagged plan deviations (setQueryData+refetchType:'none' in useCloseTicket/useReopenTicket; notifyOnChangeProps:'all' in useTicketDetail) adjudicated as acceptable AC-literate readings of implementation_plan v2 Change 5, not drift; the two invented UI details (status-filter labels, hardcoded en-US date format on TicketDetailScreen ignoring ProfileScreen's locale field) confirmed non-blocking (no FR/AC violated) but the locale inconsistency is carried forward as a real UX finding for reconciliation-reviewer. Three new /tickets* routes confirmed correctly guarded (ProtectedRoute), test names cited."
  - "COMMIT HYGIENE: frontend/src/screens/ProfileScreen.test.tsx (a US-5.2 file, not in US-5.3's task_breakdown v2 file set) was modified by TEST_WRITING attempt 3 to fix a full-suite timing flake unrelated to US-5.3's own scope. One-line, behavior-preserving test-infra fix (fireEvent.change instead of user.type()), documented in test_generation_report's Attempt 3 section, but will appear in this story's PR diff as an unexplained US-5.2 edit unless called out explicitly - AGENTS.md section 7.8. Flagged for pr-preparer/reconciliation-reviewer, not blocking."
  - "WORKFLOW-STATE HYGIENE: three history.jsonl events this run (timestamps 2026-09-12T00:35:00Z, 00:45:00Z, 00:50:00Z) recorded 'attempt' as a continuing dispatch counter (3, 4, 2) rather than state-schema.md rule 4's 'attempt resets to 1 on a PASS-advance' - discovered mid-run via advisor review and corrected going forward in workflow-state.yaml itself (which is accurate as of this write), but history.jsonl is append-only and was not rewritten. Readers comparing attempt counts across this run's history events should treat those three timestamps' attempt field as informational only, not schema-conformant."
  - "SECURITY_REVIEW (security-reviewer, attempt 1): PASS. All AGENTS.md section 7 checklist rows N/A by construction (frontend-only, no backend/auth surface touched) or Pass. Frontend-specific invariants (memory-only access token, refresh token untouched) re-confirmed unchanged. Three new-attack-surface items specific to this story checked with cited evidence: customer-authored free text (category/reply body) renders via escaped JSX interpolation only (0 dangerouslySetInnerHTML hits, backed by a real DOM-structure test), crypto.randomUUID() idempotency key is CSPRNG-backed and not a security token, 429 Retry-After surfaces only a value already visible in the standard HTTP header. No Critical/Major/Low finding."
  - "WORKFLOW-STATE HYGIENE (continued): the 2026-09-12T00:55:00Z history.jsonl event mislabeled its from_stage/to_stage pair as QUALITY_GATE->IMPLEMENTATION_VERIFICATION (duplicating gate-enforcer's own 00:50:00Z transition) when it should have read IMPLEMENTATION_VERIFICATION->SECURITY_REVIEW for implementation-verifier's own PASS. A corrective event with the right from_stage/to_stage pair (skill:null) was appended at 01:00:00Z immediately before security-reviewer's own transition event. history.jsonl is append-only; the original mislabeled event was not rewritten. This workflow-state.yaml file's own current_stage/attempt/last_result values are accurate as of each write and are the authoritative record; treat history.jsonl's 00:55:00Z line as superseded by this note, not as ground truth."
  - "RECONCILIATION (reconciliation-reviewer, attempt 1): PASS. All 14 TK-ACs plus FR-15 have matrix rows; TK-AC6/7/8/9 (the ones tied to the self-flagged plan deviations and the eslintrc-mandated export) read in full and confirmed to assert real behavior, not name-proximity. Both plan deviations and both invented UI details confirmed non-drift from an AC-compliance angle (neither contradicts nor exceeds any AC). The out-of-scope ProfileScreen.test.tsx edit is carried forward as a commit-hygiene item for pr-preparer, not treated as AC drift."
  - "STORY IS NOW AT HUMAN_PR_APPROVAL (human_gate). Required artifacts: docs/verification/US-5.3-implementation-verification.md v1, docs/reviews/security/US-5.3-security-review.md v1, docs/reviews/reconciliation/US-5.3-reconciliation.md v1, docs/reconciliation/US-5.3-traceability.md v1 - all automated_verdict PASS, zero blocking findings. /so:approve or /so:reject records the human decision; no stage skill runs until then. Two non-blocking items to disclose in the PR: the ProfileScreen.test.tsx out-of-scope test fix, and TicketDetailScreen.tsx's hardcoded en-US date format (real UX gap, no AC violated, candidate follow-up)."
  - "HUMAN_PR_APPROVAL: APPROVED (2026-09-12T18:25:25Z, sbruhov@gmail.com, no comment). All four required artifacts reconfirmed current (DRAFT, not superseded/archived) with automated_verdict PASS and zero blocking findings. Advanced to PR_PREPARATION."
  - "PR_PREPARATION (pr-preparer, attempt 1): PASS. docs/pr/US-5.3-pr-summary.md v1 (DRAFT) produced - PR title, summary linking story/spec/plan, 17-item AC-traceability test-plan checklist, risk/rollback notes, .env.example confirmed unaffected (no new setting). stale_reconciliation checked and does not apply (no frontend/src or .eslintrc.cjs file newer than the reconciliation report by mtime). Full commit-hygiene disclosure included for three known non-scope items: the human-approved eslintrc override for TicketDetailScreen.tsx, the out-of-scope ProfileScreen.test.tsx timing-flake fix, and the incidental tsconfig.app.tsbuildinfo regeneration. Also flagged: docs/specifications/US-5.3-spec.md's Open Questions/FR-12 prose still reads OD-1..OD-6 as unresolved, stale relative to implementation_plan v2's binding resolutions actually implemented - correcting the spec is us-clarifier/story-spec-writer's ownership, not this stage's."
  - "WORKFLOW-DEFINITION CHANGE + CORRECTION (2026-09-12, sbruhov@gmail.com request): a new PR_CREATION stage (skill pr-creator) was added to stage-map.yaml between PR_PREPARATION and READY_FOR_PR, per human decision to update AGENTS.md section 10's push/PR-creation principle - pr-creator is the one named, explicit-trigger-only exception, never auto-run by /so:next. This was added AFTER this run had already advanced current_stage to READY_FOR_PR on the old (pre-PR_CREATION) map. Corrected: current_stage rolled back to PR_CREATION (no PR had actually been opened yet, so no real state was lost), pending_human_gate cleared, status reset to IN_PROGRESS. history.jsonl's already-appended PR_PREPARATION->READY_FOR_PR event is not rewritten (append-only); this note plus the next transition event are the authoritative correction."

started_at: "2026-09-08T18:35:00Z"
updated_at: "2026-09-12T21:32:00Z"
completed_at: null
archived_at: null
