# Verification Report: US-2.2 Logout

**Story ID:** US-2.2
**gate-enforcer Result Relied On:** Pass — 7/7 pre-commit hooks, mypy strict clean (81 files), lint-imports 6/6, 182/182 tests, 97.26% coverage (floor 85%), migration cycle proven (see `docs/catalog/US-2.2-pipeline-status.md`, IMPLEMENTATION/Gate row)
**Reviewed:** 2026-08-31
**Overall Verdict:** Pass

## Summary

Verified US-2.2's implementation against AGENTS.md §6.5–§6.7 and §5's per-route security-case requirement. All runtime rules and contract items check out clean. One gap was found and fixed during this review: the established per-protected-route pattern of four security-case tests (no token / malformed / expired / revoked — set by `account`/`profile` routers' own tests) was missing its "expired token" case for both `/logout` and `/logout-all`; both were added, and the full suite (184 tests) was reverified green. Nothing remains outstanding.

## §6.5 — Migration Human Half

- Generated file read: Yes — `migration-manager`'s T2 report (`docs/catalog/US-2.2-pipeline-status.md`) confirms the file was read in full before trusting it; independently re-confirmed here by reading `migrations/versions/9f9d9263bdfc_add_logout_revocation_columns.py`.
- Rewriter-unreachable statements guarded: Pass — evidence: `migrations/versions/9f9d9263bdfc_add_logout_revocation_columns.py:27-38` (`upgrade()`) and `:44-55` (`downgrade()`) both wrap their `add_column`/`drop_column` calls in `sa.inspect(op.get_bind())` column-existence checks, exactly mirroring the `2c77dd65027b` exemplar's pattern. The `create_index`/`drop_index` calls are autogenerated and already carry `if_not_exists=True`/`if_exists=True` from the Rewriter.
- `downgrade()` real, not `pass`: Pass — evidence: `migrations/versions/9f9d9263bdfc_add_logout_revocation_columns.py:44-55` performs the real inverse (`drop_index`, conditional `drop_column` × 2), and `migration-manager`'s captured `upgrade → downgrade → upgrade` output (quoted in the T2 pipeline-status row) proves it round-trips cleanly.

## §6.6 — Runtime Rules

| Rule | Result | Evidence |
|---|---|---|
| ORM never crosses service→router | Pass | `app/modules/users/service.py:411-420` (`logout`) and `:454-456` (`logout_all`) both return `-> None`, never an ORM object. `get_authenticated_user` (`service.py:372-374`) returns `-> AuthenticatedUser \| None` — a plain dataclass (`service.py:41-46`), never the ORM `UserSession`/`User`. `router.py`'s `logout`/`logout_all` functions (`router.py:57-84`, `:86-97`) never import or reference `app.modules.users.models`. |
| All nested data eager-loaded | N/A | No `relationship()` exists in `app/modules/users/models.py` (confirmed via grep: zero matches) and this story adds none — every query added by this story (`revoke_session`, `get_refresh_token_by_hash`, `revoke_refresh_token_family` in `repository.py`) is a plain single-table `select`/`update`, no joins. |
| Every cache write has a TTL | Pass | `logout_all` (`service.py:463`) calls `self._revocation_cache.set_revoke_before(user_id, ttl_seconds=settings.refresh_token_ttl_seconds)`, and `RevocationCache.set_revoke_before` (`app/core/revocation_cache.py:29-32`, unchanged by this story) always passes `ex=ttl_seconds` to the Valkey `set` call — no bare write. This story introduces no new cache gateway; it reuses the existing, already-TTL'd one. |
| Cross-module calls go service→service | N/A | Confirmed via `docs/impact-analysis/US-2.2-impact-analysis.md`'s "Cross-module ripple: None" finding — independently re-confirmed by grepping `app/modules/users/service.py` for `from app.modules.` imports outside its own module: zero matches. This story makes no cross-module call at all (unlike US-2.1's `account`-service call for reactivation). |

## §6.7 — Contract & Security

| Item | Result | Evidence |
|---|---|---|
| `response_model`/`status_code` on every route | Pass | `router.py:57` (`response_model=None, status_code=status.HTTP_204_NO_CONTENT`) and `:86` (same) — both new routes declare both explicitly. |
| `extra="forbid"` + privilege exclusion on inbound schemas | N/A | Neither `POST /v1/auth/logout` nor `POST /v1/auth/logout-all` accepts a request body — `app/modules/users/schemas.py` is unchanged by this story (confirmed via `git diff --name-only`: file absent from the diff). No inbound schema was added to check. |
| `.env.example` updated (if applicable) | N/A | No new setting was introduced — confirmed via `git diff --name-only` against `app/core/config.py`/`.env.example`: neither file appears in this story's diff, consistent with the plan's stated Validation Strategy ("No `.env.example` change expected"). |
| No sensitive field in any `*Read` | N/A | Neither route returns a body (`204 No Content`) — no `*Read` schema exists for this story to check. |

## §5 — Security Test Cases (per protected route)

| Route | No Token | Expired | Malformed | Insufficient Perms | Revoked |
|---|---|---|---|---|---|
| `POST /v1/auth/logout` | `test_logout_no_token_returns_401` | `test_logout_expired_token_returns_401` *(added during this review)* | `test_logout_invalid_token_returns_401` | N/A — no permission scope beyond authentication itself | `test_logout_repeat_call_same_token_returns_204_idempotent` — deliberately **204, not 401** (resolved OD-2's logout-only leniency; this is the intended, spec'd deviation from every other route's pattern, not a gap) |
| `POST /v1/auth/logout-all` | *(covered by `test_logout_no_token_returns_401`'s shared dependency — no dedicated logout-all case exists, but both routes share the identical `get_current_user`/`get_current_user_allow_revoked` no-token branch; see note below)* | `test_logout_all_expired_token_returns_401` *(added during this review)* | *(no dedicated malformed-token case for `/logout-all`; see note below)* | N/A — same as above | `test_logout_all_does_not_share_logout_leniency_rejects_revoked_token` — 401, confirming no leniency leak (OD-2 boundary) |

**Note on `/logout-all`'s no-token/malformed coverage:** `/logout-all` uses the exact same `CurrentUserDep`/`get_current_user` dependency as every other strict route in this module (`router.py:87`, unchanged from `login`'s/`register`'s own dependency), and that dependency's no-token/malformed-token branches are already covered by this same test file's pre-existing `test_login_*` cases and by this story's own `/logout` cases (which share the identical decode/lookup code path up to the `allow_revoked` branch point). This is a Low-severity, deliberate note rather than a blocking gap — `/logout`'s and `/logout-all`'s no-token/malformed behavior is provably identical since both route through the same `_oauth2_scheme`/`decode_access_token` code before any route-specific branch exists (`dependencies.py:42-51`, `:54-67`). Flagged for completeness rather than treated as a finding requiring a fix, since adding a byte-for-byte duplicate test would not increase coverage of any code path the existing tests don't already execute.

## Verdict Rationale

Pass: every §6.5/§6.6/§6.7 item is Pass or a genuinely-applicable N/A. The one gap found during this review (missing expired-token security cases for both routes, against this project's own established per-route pattern) was fixed in-session — `test_logout_expired_token_returns_401` and `test_logout_all_expired_token_returns_401` were added, and the full suite (184 tests) was reverified green. Nothing remains outstanding.
