# Pipeline Status: US-2.2

**Active story confirmed:** Yes — user explicitly requested continuing with US-2.2 on 2026-08-31, after US-2.1 (previous active story) reached PR, was committed, and was merged as PR #2.
**Last updated:** 2026-08-31

| Stage | Sub-step | Skill | Status | Verdict | Notes |
|---|---|---|---|---|---|
| CLARIFICATION | — | us-clarifier | Done | **Ready for Specification** (was Not Ready, resolved by user 2026-08-31) | docs/decisions/US-2.2-open-decisions.md (6 items, all resolved — recommended option accepted throughout), docs/evidence/US-2.2-clarification-report.md. A spec+review (US-006) already existed outside this pipeline (2026-08-22, Pass with Issues); reading the now-implemented US-2.1 codebase surfaced real architecture divergences (OD-1–OD-5) the pre-existing spec couldn't have known about. |
| SPECIFICATION | — | story-spec-writer | Done | — | docs/specifications/US-006-logout-spec.md revised 2026-08-31 to incorporate OD-1–OD-6 (new "Open Decision Resolutions" section; FR-1/FR-4/FR-5 rewritten for the UserSession.revoked_at mechanism and logout-only idempotency carve-out; CSRF moved to Out of Scope) |
| SPEC_REVIEW | — | story-spec-reviewer | Done | **Pass with Issues**, accepted 2026-08-31 | docs/reviews/specifications/US-006-spec-review.md. Prior [High] LO-AC4/LO-AC5 ambiguity resolved via OD-2. Lookup-miss finding resolved by user (silent skip, still 204) — spec FR-1 amended. Remaining [Low] scope-creep note (OD-anchored content) does not block. |
| DESIGN | API | openapi-designer | Done | — (2 Open Questions logged, deferred to PLANNING) | docs/designs/api/US-006-openapi.yaml, US-006-api-design.md. OD-2's logout-only leniency needs a dependency-injection design decision (per-route auth exception) flagged for planner. |
| DESIGN | DB | db-designer | Done | — | docs/designs/database/US-006-db-design.md, US-006-entity-model.md. `refresh_tokens.revoked_at` + `ix_refresh_tokens_family_id` (new); `auth_audit_log.scope` (new). `user_sessions` unchanged (reuses existing `revoked_at`). |
| PLANNING | Impact analysis | impact-analyzer | Done | — | docs/impact-analysis/US-006-impact-analysis.md. No cross-module ripple (unlike login/US-2.1, no account.service call needed). 3 new repository methods, 1 changed signature (create_auth_audit_log_entry), new logout()/logout_all() service methods, 1 new dependency for the allow_revoked carve-out. 3 additive nullable migration changes. |
| PLANNING | Plan | planner | Done | — | docs/plans/US-006-implementation-plan.md. New `allow_revoked` param + dedicated `get_current_user_allow_revoked` dependency (isolates OD-2's leniency to /logout only). No new module/cache.py/error class. Additive-only migration. |
| PLANNING | Task breakdown | implementation-planner | Done | — | docs/plans/US-006-task-breakdown.md. T1-T7, mostly linear (no independent T1∥T2 schema task this time). No unowned T0-style infra tasks (unlike US-2.1) — nothing touches app/core/* or app/main.py. |
| PLANNING | Plan review | plan-reviewer | Done | Pass with Issues (1 Low finding, fixed same-day) | docs/reviews/plans/US-006-plan-review.md. T1/T3 boundary ambiguity (create_auth_audit_log_entry caller update) fixed directly in task-breakdown.md T3. |
| TESTS | — | test-writer | Done | — | docs/tests/US-006-traceability-matrix.md (traceability matrix only; test code deferred to IMPLEMENTATION per task-breakdown sequencing, same as US-2.1). Deliberate LO-AC5/OD-2 asymmetry (/logout leniency vs /logout-all standard) documented explicitly instead of one uniform baseline row. |
| IMPLEMENTATION | Schemas | schema-builder | N/A | — | Not needed: both endpoints are 204/no-body (no request or response schema). |
| IMPLEMENTATION | Data layer (T1) | data-layer-builder | Done | imports clean | users/models.py (`RefreshToken.revoked_at`+`family_id` index, `AuthAuditLog.scope`), users/repository.py (`revoke_session`, `get_refresh_token_by_hash`, `revoke_refresh_token_family`, `create_auth_audit_log_entry` gains `scope`). mypy clean, no session.query(). Expected transient protocol-mismatch in dependencies.py until T3. |
| IMPLEMENTATION | Migration (T2) | migration-manager | Done, **verified** | upgrade/downgrade/upgrade proven clean via a standalone Postgres container | migrations/versions/9f9d9263bdfc_add_logout_revocation_columns.py. Confirmed resulting schema matches DB design (`refresh_tokens.revoked_at`, `ix_refresh_tokens_family_id`, `auth_audit_log.scope`) via `\d` on both tables. |
| IMPLEMENTATION | Service (T3) | service-and-router-builder | Done | imports clean | users/service.py: new `logout()`/`logout_all()`; `get_authenticated_user` gains `allow_revoked`; `UserRepositoryProtocol`/`RevocationCacheReaderProtocol`→`RevocationCacheProtocol` updated; 5 existing login-flow `create_auth_audit_log_entry` call sites updated with `scope=None`. Found+fixed a real gap: needed a `hash_refresh_token()` helper for looking up a presented cookie (extracted from `generate_refresh_token`) — added to `app/core/security.py` directly (unowned-infra pattern, same as US-2.1's T0c), not anticipated in impact-analysis/plan. mypy strict clean, zero fastapi/starlette/HTTPException imports. |
| IMPLEMENTATION | Router/Deps (T4) | service-and-router-builder | Done | routes registered, imports clean | users/router.py (`POST /auth/logout`, `POST /auth/logout-all`, both 204/no-body), users/dependencies.py (`get_current_user_allow_revoked`, used by exactly one router function — verified via grep, the plan's highest-severity risk mitigation). mypy strict clean, zero sqlalchemy/models/repository imports. |
| IMPLEMENTATION | Tests (T5, T6) | test-writer | Done, **verified** | 11/11 unit + 9/9 integration pass on real Postgres+Valkey (testcontainers); full suite 182/182 | tests/unit/modules/users/test_users_service.py (extended), tests/integration/modules/users/test_users_router.py (extended). Found+fixed a pre-existing test-infra gap during T6: `tests/conftest.py`'s `client`/`real_client` fixtures used `base_url="http://test"`, and httpx silently drops a `Secure` cookie replayed over plain-http even via in-process `ASGITransport` — this story is the first test to chain the login cookie into a second request. Fixed by changing both fixtures to `base_url="https://test"` (no real TLS involved either way); full suite reverified green. |
| IMPLEMENTATION | Gate (T7) | gate-enforcer | Done | **PASS** — 7/7 pre-commit hooks green, mypy strict clean (81 files), lint-imports 6/6, 182/182 tests, 97.26% coverage (floor 85%), migration cycle proven | Found+fixed 2 unanticipated implementation gaps (hash_refresh_token helper; Secure-cookie test-fixture base_url bug in conftest.py) and 3 stale `.gitignore` files (docs/evidence, docs/reviews/reconciliation, docs/reviews/security) silently excluding pipeline docs from git — same class of bug as US-2.1's PR fix. One pre-existing flaky unit test (JWT-tamper test, unrelated to this diff, confirmed via isolation + 3x full-suite reruns all green). |
| VERIFICATION | — | implementation-verifier | Done | Pass | docs/verification/US-006-verification-report.md. Found+fixed one gap: missing expired-token security cases for both routes (project's own 4-case-per-route precedent from account/profile). 2 tests added, full suite 184/184 reverified. |
| SECURITY_REVIEW | — | security-reviewer | Done | **Pass** | docs/security/US-006-security-review.md. All 6 §7 rows Pass/N/A. 1 [Low] advisory (CSRF not enforced — disclosed OD-4, not a new gap). |
| RECONCILIATION | — | reconciliation-reviewer | Done | Pass (1 gap found+fixed same-day: LO-AC4's idempotency test didn't independently verify "no additional revocation side effects" — now asserts persisted revoked_at unchanged across the repeat call) | docs/reconciliation/US-006-reconciliation-report.md. Also fixed a stale traceability-matrix cross-reference for the expired-token test. |
| PR | — | pr-preparer | Done | Draft ready | docs/pr/US-006-pr-description.md. Found+fixed 3 more stale `.gitignore` files (docs/evidence, docs/reviews/reconciliation, docs/reviews/security). `docs/evidence/US-2.1-clarification-report.md` left untracked, deliberately excluded (US-2.1's own doc, only surfaced by this story's fix). |
| POST-PR | Independent review | advisor | Done | **Found+fixed a real IDOR** | 2026-09-01: `UserService.logout()` revoked a hash-matched refresh-token family without checking `existing.user_id == user_id` — a stolen refresh token let any authenticated caller silently kill a victim's whole session family. Fixed in `app/modules/users/service.py`; 2 new regression tests (unit+integration, `test_logout_cross_user_refresh_cookie_does_not_revoke_other_users_family`); addenda added to `docs/security/US-006-security-review.md` and `docs/reconciliation/US-006-reconciliation-report.md`; traceability matrix and PR draft updated. Full suite reverified 186/186, mypy strict clean, pre-commit green. |

## Blocking Stage (if any)

None. Story reached PR with every stage Pass, plus a post-PR IDOR found and fixed via independent review. Draft ready for user review at `docs/pr/US-006-pr-description.md`; nothing staged, committed, or pushed yet.
