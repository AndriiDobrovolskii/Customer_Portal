# Pipeline Status: US-2.5

**Active story confirmed:** Yes — user explicitly requested continuing with US-2.5 on 2026-09-01, right after US-3.2 (its blocking dependency) reached PR (#9).
**Last updated:** 2026-09-01

| Stage | Sub-step | Skill | Status | Verdict | Notes |
|---|---|---|---|---|---|
| CLARIFICATION | — | us-clarifier | Done (re-run) | **Ready for Specification** (all 11 ODs resolved 2026-09-01) | docs/decisions/US-2.5-open-decisions.md (11 items, all resolved — recommended option accepted throughout except OD-5, user chose the non-default degraded-state mechanic). OD-1 (BLOCKING) resolved: US-3.2 reached PR #9, merged same day. New OD-7: MF-AC6's "US-3.2 MR-AC1" citation is fictional. docs/evidence/US-2.5-clarification-report.md updated in place. |
| — | Branch/merge | — | Done | — | US-3.2 PR #9 merged to `main` (d3b76dd) per user decision. `feat/us-2.5-mfa-totp` branched from `main` (not stacked), carrying the CLARIFICATION artifacts forward as uncommitted changes. |
| SPECIFICATION | — | story-spec-writer | Done | — | docs/specifications/US-2.5-spec.md revised in place (pre-existing 2026-08-22 draft) — new "Open Decision Resolutions" section (all 11 ODs), FR-6 folds in the enrolment-scoped-token mechanism (OD-4/OD-7, two triggers per OD-5), FR-8 is wholly new (DELETE success path, OD-6/OD-8, no source AC). 7/7 source ACs traced. No Open Questions remain. |
| SPEC_REVIEW | — | story-spec-reviewer | Done | **Pass with Issues**, resolved 2026-09-01 | docs/reviews/specifications/US-2.5-spec-review.md. 7/7 ACs Covered, no contradictions. 1 High (grace-period clock had no data source) + 2 Medium ambiguities resolved by the user and folded into the spec: `user_roles.granted_at` column added, no grace period for the recovery-code trigger, refresh re-evaluates/re-scopes. 3 Low disclosed-scope-creep findings noted (non-blocking, all traced to user-approved ODs). |
| DESIGN | API | openapi-designer | Done | — | docs/designs/api/US-2.5-{openapi.yaml,api-design.md}. 4 new endpoints (enroll/activate/verify/disable) + additive extension to existing login/refresh (FR-6). 4 Open Questions deferred to PLANNING. |
| DESIGN | DB | db-designer | Done | — | docs/designs/database/US-2.5-{db-design,entity-model}.md. 4 new `users` columns, new `mfa_recovery_codes` table, 1 new column (`granted_at`) on the already-merged US-3.2 `user_roles` table. No `auth_audit_log` schema change needed. |
| PLANNING | Impact analysis | impact-analyzer | Done | — | docs/impact-analysis/US-2.5-impact-analysis.md. Extends `users` module, no new module. 2 open architectural tensions flagged (enrolment-scoped-token mechanism, `mfa_token` mechanism). |
| PLANNING | Plan | planner | Done | — | docs/plans/US-2.5-implementation-plan.md. Both tensions resolved: JWT claim + default-deny at `get_authenticated_user` (mirrors `get_current_user_allow_revoked`); Valkey-backed opaque `mfa_token` via `GETDEL`. New `app/core/crypto.py` for AES-GCM (OD-2). |
| PLANNING | Task breakdown | implementation-planner | Done | — | docs/plans/US-2.5-task-breakdown.md. T1-T9: T1(schemas)∥T2(users data)∥T2b(roles data) → T3(migration) → T4(roles service) → T5(users service+core) → T6(users router) → T7∥T8(tests) → T9(gate). |
| PLANNING | Plan review | plan-reviewer | Done | **Pass with Issues** (1 Low + 1 Medium, resolved same-day) | docs/reviews/plans/US-2.5-plan-review.md. Full impact-analysis coverage, correct §3 layering, no scope creep. Fixed: `app/api/v1/router.py` no-change confirmed explicitly; `mfa_token`/replay-key concurrency shape confirmed benign (GETDEL atomicity). |
| TESTS | — | test-writer | Done | — | docs/tests/US-2.5-ac-test-matrix.md (matrix only; test code deferred to IMPLEMENTATION T7/T8). Covers MF-AC1-7, FR-8's no-AC disable path, and dedicated default-deny enrolment-scoping coverage (both directions + 2 full-flow integration tests). |
| IMPLEMENTATION | T1 schemas | schema-builder | Done | mypy clean | users/schemas.py: 8 new MFA schemas, LoginResponse/RefreshResponse gain mfa_enrollment_deadline. |
| IMPLEMENTATION | T2 users data | data-layer-builder | Done | mypy clean | users/models.py (4 new User columns, MfaRecoveryCode), repository.py (8 new methods), cache.py (MfaTokenCache, MfaReplayCache), cache_keys.py (3 new keys - moved here from planned T5 per US-2.4 precedent). |
| IMPLEMENTATION | T2b roles data | data-layer-builder | Done | mypy clean | roles/models.py (UserRole.granted_at), repository.py (list_role_grants_for_user, explicit granted_at write). |
| IMPLEMENTATION | T3 migration | migration-manager | Done, **verified** | upgrade/downgrade/upgrade proven against real Postgres (customer_portal_pg) | migrations/versions/cef55228a927_add_mfa_columns_and_recovery_codes.py. 4 users columns + mfa_recovery_codes (Rewriter-guarded) + user_roles.granted_at (hand-guarded add_column). |
| IMPLEMENTATION | T4 roles service | service-and-router-builder | Done | mypy clean, zero fastapi imports | roles/service.py: get_role_grants_for_user + RoleGrant NamedTuple. |
| IMPLEMENTATION | T5 users service+core | service-and-router-builder | Done | mypy clean, zero fastapi imports | New app/core/crypto.py (AES-GCM, added `cryptography` dependency via uv). TOTP primitives in security.py. 4 new config settings. users/service.py: enroll/activate/verify/disable_mfa, default-deny enrolment-scoping in get_authenticated_user, login/refresh both re-evaluate scoping. 3 new exceptions. New EmailSender.send_mfa_recovery_used_notice (rippled to 2 other modules' test fakes). |
| IMPLEMENTATION | T6 users router | service-and-router-builder | Done | routes registered (confirmed via app.openapi()), zero sqlalchemy imports | users/router.py: 4 new routes; login response is now LoginResponse\|MfaRequiredResponse with conditional cookie. New CurrentUserAllowEnrollmentScopedDep. |
| IMPLEMENTATION | T7 unit tests | test-writer | Done, **verified** | 211/211 unit tests pass, mypy strict clean | New test_crypto.py; extended test_security.py, test_roles_service.py, test_users_service.py. Fixed EmailSender Protocol ripple in email_verification/profile fakes. |
| IMPLEMENTATION | T8 integration tests | test-writer | Done, **verified** | 136/136 integration tests pass (real Postgres+Valkey) | New tests/integration/modules/users/test_mfa_router.py (15 tests). Fixed 1 pre-existing exact-body-keys assertion for RefreshResponse's new field. One full-flow test scoped down due to db_session fixture's single-transaction Postgres now() limitation (same as US-3.2's own MR-AC2 precedent). |
| IMPLEMENTATION | T9 gate | gate-enforcer | Done | **PASS** — 7/7 pre-commit hooks, mypy strict clean, import-linter 6/6, 347/347 tests, 97.35% coverage (floor 85%; service.py 98%, router.py 100%), migration cycle proven | Found+fixed along the way: 1 detect-secrets false positive (hex-looking recovery-code test literal), 1 unused test-helper parameter, ruff-format auto-fix on 4 files. |
| VERIFICATION | — | implementation-verifier | Done | **Pass** (1 gap found+fixed same-day) | docs/verification/US-2.5-implementation-verification.md. ORM containment/cross-module/cache-TTL/contract checks all Pass; eager-loading N/A. SS5 gap: 13 missing security-case tests (activate/disable had none) added, full suite 360/360. |
| SECURITY_REVIEW | — | security-reviewer | Done | **Pass** (1 finding found+fixed same-day) | docs/reviews/security/US-2.5-security-review.md. Rule 2 (no reversible encryption for credentials) reasoned through explicitly: AES-GCM on the TOTP secret is correct (must be recomputed-from, not compared-against) vs. recovery codes correctly Argon2id-hashed. Rule 6 found a real defect: `disable_mfa`'s sequential password-then-code check let a stolen bearer session distinguish which factor was wrong via the response body (an oracle, given the endpoint's joint-two-factor threat model). Fixed same-day: both factors now evaluated unconditionally, one exception type on either failure, regression-guarded by a new test. Gate re-confirmed green: 7/7 pre-commit, mypy clean, 361/361 tests, 97.35% coverage. 3 Low advisories, none forcing Fail. |
| RECONCILIATION | — | reconciliation-reviewer | Done | **Pass** (7 gaps found+fixed same-day) | docs/reviews/reconciliation/US-2.5-reconciliation.md. All 7 ACs + FR-8 confirmed full matrix coverage after closing 7 gaps (7 matrix-named test functions didn't exist under any name; a further ~dozen renamed-but-equivalent rows individually verified, not gaps). None was a live bug. Closed with 6 new tests: `granted_at` write now proven against real DB (FR-6's grace-period-clock data source); enroll-accepts/disable-rejects scoped-token allow-list boundary both proven; OD-10's shared lockout counter proven for a recovery-code guess; both-triggers-true precedence proven; replay protection proven against real Valkey. No spec drift. Gate re-confirmed green: 367/367 tests, 97.35% coverage. Advances to PR. |
| PR | — | pr-preparer | Done | **Opened as PR #10** | docs/pr/US-2.5-pr-summary.md. All 4 gates confirmed Pass from their actual reports. `.env.example` alignment and commit hygiene both confirmed clean. Committed as `ae8d2a2` (one detect-secrets false positive fixed first — a pre-existing test literal's line number shifted, re-flagged, `pragma: allowlist secret` added), pushed to origin, opened as https://github.com/AndriiDobrovolskii/Customer_Portal/pull/10. |
