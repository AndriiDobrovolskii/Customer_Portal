# Pipeline Status: US-3.1

**Active story confirmed:** Yes — user explicitly requested continuing to US-3.1 on 2026-09-02, right after US-2.6 reached PR #11 (confirmed MERGED to `main`).
**Last updated:** 2026-09-02

| Stage | Sub-step | Skill | Status | Verdict | Notes |
|---|---|---|---|---|---|
| — | Branch | — | Done | — | `feat/us-3.1-manage-users` branched from `origin/main` (`ac206b1`, confirmed via `git branch -r --contains fb4f6a9` that US-2.6's merge commit is an ancestor). Corrected mid-session: docs work was started on the stale `feat/us-2.6-active-sessions` branch before this was caught; `git stash push -u`, branched fresh from `origin/main`, `git stash pop` — clean, no conflicts, confirmed via diff. |
| CLARIFICATION | — | us-clarifier | Done | **Not Ready → Resolved** (all 3 ODs resolved 2026-09-02) | docs/decisions/US-3.1-open-decisions.md (OD-1 `admin_audit_log`'s US-3.2-built shape doesn't fit MU-AC9/FR-9; OD-2 `account_lifecycle_audit_log` has no `reason` column for MU-AC13/FR-13; OD-3 `GET /v1/admin/users/{id}` in-scope with zero ACs), all resolved, recommended option accepted throughout. 6 of the pre-existing US-3.1 spec's/review's open items resolved outright by reading the real US-1.4/US-3.2 codebase. docs/evidence/US-3.1-clarification-report.md. |
| SPECIFICATION | — | story-spec-writer | Done | — | docs/specifications/US-3.1-spec.md revised in place (pre-existing 2026-08-22 draft). Incorporates OD-1–OD-3 plus new FR-17b/FR-22/FR-23 with no source AC. 21/21 source ACs traced. |
| SPEC_REVIEW | — | story-spec-reviewer | Done | **Pass with Issues**, accepted as-is | docs/reviews/specifications/US-3.1-spec-review.md. 21/21 ACs Covered, no contradictions. 3 Low findings, all disclosed/precedent-grounded (FR-6's BR-001 concurrency sentence; FR-17b/FR-22/FR-23 lacking a source AC, matching US-2.6 FR-6/FR-7 precedent). User accepted without further changes. |
| DESIGN | API | openapi-designer | Done | — | docs/designs/api/US-3.1-{openapi.yaml,api-design.md}. 7 operations across 6 paths. ETag/If-Match mechanics reused verbatim from `app/core/etag.py` + `app/modules/profile`'s precedent. 9 Open Questions logged; 1 (PATCH's missing `reason` field) resolved by `planner`; 1 (DELETE 401-vs-405 tension) resolved during plan-review. |
| DESIGN | DB | db-designer | Done | — | docs/designs/database/US-3.1-{db-design,entity-model}.md. New `invitation_tokens` table (matches existing token precedent); `admin_audit_log` +4 nullable columns (OD-1); `account_lifecycle_audit_log` +1 nullable column (OD-2); 2 new `users` indexes incl. this codebase's first `pg_trgm` use. 3 Known Gaps logged; 1 (PATCH `reason` field) resolved by `planner`, 1 (`admin_audit_log.actor_id` mapping) clarified during plan-review. |
| PLANNING | Impact analysis | impact-analyzer | Done | — | docs/impact-analysis/US-3.1-impact-analysis.md. New module `app/modules/admin_users/` (mirrors `profile`/`roles` precedent, no `users` module change). 2 new cross-module `RoleService` calls flagged. |
| PLANNING | Plan | planner | Done | — | docs/plans/US-3.1-implementation-plan.md. Resolves PATCH's missing `reason` field (amends `US-3.1-openapi.yaml`), FR-18's token-invalidation mechanism (reuse `consumed_at`), and adds 2 new `RoleService` public methods for FR-8/FR-16 reuse. |
| PLANNING | Task breakdown | implementation-planner | Done | — | docs/plans/US-3.1-task-breakdown.md. T1(schemas)∥T2(admin_users data)∥T2b(audit columns)∥T4(roles service) → T3(migration)→T3b(concurrent index, own migration) → T5(admin_users service) → T6(admin_users router) → T7∥T8(tests) → T9(gate). |
| PLANNING | Plan review | plan-reviewer | Done | **Pass with Issues** (4 findings, resolved same-day) | docs/reviews/plans/US-3.1-plan-review.md. Full impact-analysis coverage, correct §3 layering. Fixed: T3's migration bundled a `CONCURRENTLY` index with transactional DDL (split into T3/T3b, per US-2.6's own precedent); `migrations/env.py` touch was assigned to a skill outside its declared scope (now unassigned/out-of-band, per US-3.2's precedent); advisor cross-check caught a genuine behavior-change bug in the original `raise_if_last_admin`/`replace_user_roles` wiring (fixed: additive-only, not called from `replace_user_roles`); 2 smaller design clarifications (`admin_audit_log.actor_id` mapping; DELETE 401-vs-405 reading) closed in the same pass. |
| TESTS | — | test-writer | Done | — | docs/tests/US-3.1-ac-test-matrix.md (matrix only, test code deferred to T7/T8). All 21 ACs + FR-17b/FR-22/FR-23 covered; MU-AC16/BR-001 concurrency tests; 2 regression-guard tests for the plan-review-corrected `RoleService` wiring. |
| IMPLEMENTATION | T1 schemas | schema-builder | Done | mypy clean | admin_users/schemas.py: `UserRead`, `UserListResponse`, `CreateUserRequest`, `UpdateUserRequest` (required `reason`), `DeactivateUserRequest`, `ResendInviteResponse`. |
| IMPLEMENTATION | T2 data layer | data-layer-builder | Done | mypy clean | admin_users/{models.py (`InvitationToken`), repository.py} — cursor-paginated `list_users`, `get_with_roles`, `create` (writes initial `UserRole` rows), `update_fields`, `deactivate_if_active`, audit-log writers, invitation-token CRUD. |
| IMPLEMENTATION | T2b audit columns | data-layer-builder | Done | mypy clean | roles/models.py (`AdminAuditLog` +4 nullable cols), account/models.py (`AccountLifecycleAuditLog` +1 nullable col). |
| IMPLEMENTATION | T4 roles service | service-and-router-builder | Done | mypy clean, zero fastapi imports | roles/service.py: `resolve_role_ids_for_grant`, `raise_if_last_admin` — both independent, neither wired into `replace_user_roles` (design simplified from the reviewed plan: no refactor of that method at all, lower regression risk). |
| IMPLEMENTATION | Plan correction | — | Done | — | `impact-analysis`/task-breakdown missed that `users.status`+`created_at` composite index needed a `users/models.py` change — caught during T3 autogenerate (no diff detected), fixed directly. |
| IMPLEMENTATION | T3 migration | migration-manager | Done, **verified** | upgrade→downgrade→upgrade proven against real Postgres | `a5edc35c8e96`: `invitation_tokens` table, 5 nullable audit columns, `ix_users_status_created_at` — guarded `add_column`s (Rewriter doesn't reach them). |
| IMPLEMENTATION | T3b migration | migration-manager | Done, **verified** | upgrade→downgrade→upgrade proven against real Postgres | `1b2b1d52dd71`: `CREATE EXTENSION pg_trgm` + 2 concurrent GIN trigram indexes (`email`, `display_name`), own migration per the plan-review finding, `autocommit_block()`. |
| IMPLEMENTATION | T5 admin_users service | service-and-router-builder | Done | mypy clean, zero fastapi imports | service.py: `AdminUserService` (6 methods). Discovered gaps fixed inline: `EmailSender` needed `send_invitation_email` (+ 3 test fakes updated); `RevocationCache` needed injecting for FR-13's `revoke_before`; 2 new config settings + `.env.example`. |
| IMPLEMENTATION | T6 admin_users router | service-and-router-builder | Done | routes registered (confirmed via `app.openapi()`), zero sqlalchemy imports | router.py (7 routes), dependencies.py; app/api/v1/router.py registration. |
| IMPLEMENTATION | Regression check | — | Done | **all green** | Full suite 409/409 (243 unit + 166 integration), mypy strict clean (109 files), `lint-imports` 6/6, ruff clean, `python -c "from app.main import app"` succeeds. |
| IMPLEMENTATION | T7 unit tests | test-writer | Done | 31 new admin_users + 7 new roles tests, all pass | tests/unit/modules/admin_users/test_admin_users_service.py (new); tests/unit/modules/roles/test_roles_service.py (extended, incl. 2 regression-guard tests). |
| IMPLEMENTATION | T8 integration tests | test-writer | Done, **2 real defects found+fixed** | 38 new tests, all pass (+ full suite re-confirmed green after fixes) | tests/integration/modules/admin_users/test_admin_users_router.py (new). Found: (1) genuine concurrency bug in the shared last-admin check (fixed in roles/repository.py, benefits US-3.2 too); (2) test-isolation gap in this story's own new concurrency-test helpers (fixed via `cleanup_users`). |
| IMPLEMENTATION | T9 gate | gate-enforcer | Done | **PASS** — 7/7 pre-commit hooks, mypy strict clean (113 files), import-linter 6/6, 485/485 tests (was 409), 96.64% coverage (floor 85%; admin_users/service.py 96%, admin_users/router.py 100%) | `.secrets.baseline` line-number drift only (accepted). |
| VERIFICATION | — | implementation-verifier | Done | **Pass** (1 gap found+fixed same-day) | docs/verification/US-3.1-implementation-verification.md. 5/7 protected routes were missing the full §5 security-case set (expired/malformed/revoked/insufficient-permission) at the integration level - 24 tests added, 509/509 green. ORM containment, cache TTL, service→service discipline, §6.7 contract items all Pass. |
| SECURITY_REVIEW | — | security-reviewer | Done | **Pass** | docs/reviews/security/US-3.1-security-review.md. All 6 AGENTS.md §7 rows Pass (3 N/A - no password/credential handling in this story). 2 Low advisories, neither forcing Fail. |
| RECONCILIATION | — | reconciliation-reviewer | Done | **Pass** (initial Fail, 2 gaps found+fixed same-day) | docs/reviews/reconciliation/US-3.1-reconciliation.md. All 24 ACs/FRs fully covered and asserted after fixing MU-AC9/MU-AC18's unasserted `actor` audit field. 29 harmless test-name renames individually verified. No spec drift. 509/509 green. |
| PR | — | pr-preparer | Done | **Merged-ready, opened** | docs/pr/US-3.1-pr-summary.md. All 4 gates confirmed Pass. Committed (6 commits, 655dc60..ba0587c), pushed to origin, opened as [GitHub PR #12](https://github.com/AndriiDobrovolskii/Customer_Portal/pull/12). User explicitly authorized ("go"). |

