# Pipeline Status: US-2.6

**Active story confirmed:** Yes — user explicitly requested continuing to US-2.6 on 2026-09-02, right after US-2.5 reached PR #10 (confirmed MERGED to `main`).
**Last updated:** 2026-09-02

| Stage | Sub-step | Skill | Status | Verdict | Notes |
|---|---|---|---|---|---|
| — | Branch | — | Done | — | `feat/us-2.6-active-sessions` branched from up-to-date `main` (confirmed the prior branch `feat/us-2.5-mfa-totp` was fully merged before branching). |
| CLARIFICATION | — | us-clarifier | Done | **Ready for Specification** (all 4 ODs resolved 2026-09-02) | docs/decisions/US-2.6-open-decisions.md (4 ODs — own-current-session revocation, 20-family cap eviction, device/browser label derivation, geo-IP mechanism — all resolved, recommended option accepted throughout). No blocking dependency (US-2.3/US-2.2 already merged). 3 of the pre-existing US-2.6 spec's 6 Open Questions resolved outright by reading US-2.3's actual shipped implementation. docs/evidence/US-2.6-clarification-report.md. |
| SPECIFICATION | — | story-spec-writer | Done | — | docs/specifications/US-2.6-spec.md revised in place (pre-existing 2026-08-22 draft). Incorporates OD-1-OD-4 plus 2 new FRs (FR-6 self-revoke 409, FR-7 cap eviction) with no source AC. 5/5 source ACs traced. |
| SPEC_REVIEW | — | story-spec-reviewer | Done | **Pass with Issues**, resolved same day | docs/reviews/specifications/US-2.6-spec-review.md. 5/5 ACs Covered, no contradictions. 2 Medium findings (current-session identification mechanism; concurrent-login cap-eviction race) resolved by the user and folded into the spec: read the `refresh_token` cookie; row-lock the count-and-evict check. 4 disclosed Low scope-creep findings noted, non-blocking. |
| DESIGN | API | openapi-designer | Done | — | docs/designs/api/US-2.6-{openapi.yaml,api-design.md}. 2 endpoints, 1 new error slug (`current-session`, 409). |
| DESIGN | DB | db-designer | Done | — | docs/designs/database/US-2.6-{db-design,entity-model}.md. No new `refresh_tokens` columns (all already exist); 1 new composite index; 1 new column `auth_audit_log.target_family`. Rejected a first-class `refresh_token_families` table as scope beyond the story. |
| PLANNING | Impact analysis | impact-analyzer | Done | — | docs/impact-analysis/US-2.6-impact-analysis.md. Extends `users` module, no new module, no cross-module ripple. |
| PLANNING | Plan | planner | Done | — | docs/plans/US-2.6-implementation-plan.md. Flagged (and later resolved by the user) 2 new pyproject.toml dependencies and the GeoLite2 distribution method. |
| PLANNING | Task breakdown | implementation-planner | Done | — | docs/plans/US-2.6-task-breakdown.md. T1(schemas)∥T2(users data)∥T4(core primitives) → T3(migration) → T5(users service) → T6(users router) → T7∥T8(tests) → T9(gate). |
| PLANNING | Plan review | plan-reviewer | Done | **Pass with Issues** (3 findings, resolved same-day) | docs/reviews/plans/US-2.6-plan-review.md. Full impact-analysis coverage, correct §3 layering. Fixed: impact-analysis.md was missing `config.py`/`.env.example`; plan's own Files-To-Modify bullet undercounted settings; migration risk didn't address the new index's `CREATE INDEX CONCURRENTLY` need on a write-heavy table. |
| TESTS | — | test-writer | Done | — | docs/tests/US-2.6-ac-test-matrix.md (matrix only). Covers SM-AC1-5, FR-6/FR-7 (no-AC), and both spec-review resolutions (cookie mechanism, row-locked eviction). |
| IMPLEMENTATION | T1 schemas | schema-builder | Done | mypy clean | users/schemas.py: `SessionEntry`, `SessionListResponse`. |
| IMPLEMENTATION | T2 data layer | data-layer-builder | Done | mypy clean | users/models.py (composite index, `AuthAuditLog.target_family`), repository.py (4 new methods incl. `get_any_refresh_token_for_family`, discovered while building the service). |
| IMPLEMENTATION | T4 core primitives | service-and-router-builder | Done | mypy clean | New app/core/geoip.py, device.py. config.py gains `max_live_sessions_per_user`, `geoip_database_path`. Added `user-agents`+`geoip2` via uv (user-approved) plus a `[[tool.mypy.overrides]]` entry for `user-agents`' missing stubs (mirrors the existing `testcontainers` precedent). |
| IMPLEMENTATION | T3 migration | migration-manager | Done, **verified** | upgrade→downgrade→downgrade→upgrade→upgrade proven against real Postgres | 2 migrations: `5dccea7a3749` (guarded `add_column`/`drop_column`) and `db8cbd5e3697` (`CREATE INDEX CONCURRENTLY` in its own migration, `autocommit_block()`, per the plan-review finding). |
| IMPLEMENTATION | T5 users service | service-and-router-builder | Done | mypy clean, zero fastapi imports | `list_sessions`/`revoke_session`, `_resolve_current_family_id`, `_evict_oldest_family_if_at_cap` called from **both** family-creation call sites (`authenticate_user` and `_complete_mfa_login` — the second site found while building the service, not caught by the plan). 2 new exceptions. |
| IMPLEMENTATION | T6 users router | service-and-router-builder | Done | routes registered (confirmed via `app.openapi()`), zero sqlalchemy imports | 2 new routes, both reading the optional `refresh_token` cookie; no `dependencies.py` change needed. |
| IMPLEMENTATION | T7 unit tests | test-writer | Done, **verified** | 243/243 unit tests pass, mypy strict clean | 30 new tests in test_users_service.py + 4 in test_geoip.py + 3 in test_device.py. Fixed `FakeUserRepository` ripple (`target_family` param, 4 new protocol methods). |
| IMPLEMENTATION | T8 integration tests | test-writer | Done, **verified** | 166/166 integration tests pass (real Postgres+Valkey) | 13 new tests in test_users_router.py, including a genuine concurrent-login race test (`real_client`+`asyncio.gather`) proving the row lock holds. |
| IMPLEMENTATION | T9 gate | gate-enforcer | Done | **PASS** — 7/7 pre-commit hooks, mypy strict clean (101 files), import-linter 6/6, 409/409 tests, 97.42% coverage (floor 85%; service.py 97%, router.py/geoip.py/device.py 100%), both migrations proven | Found+fixed along the way: 1 ruff C416 false-positive (mypy strict needs the dict-comprehension form ruff wanted rewritten), 2 long lines, 1 unused test variable, `.secrets.baseline` line-number drift. |
| VERIFICATION | — | implementation-verifier | Done | **Pass** | docs/verification/US-2.6-implementation-verification.md. ORM containment/cross-module/§6.7 contract checks all Pass; eager-loading and cache-TTL both genuine N/A. Self-corrected mid-review: an initial malformed grep wrongly suggested a revoked-session test gap; re-checked and found existing coverage. |
| SECURITY_REVIEW | — | security-reviewer | Done | **Pass** | docs/reviews/security/US-2.6-security-review.md. All 6 AGENTS.md §7 rows Pass (3 N/A — no credential handling or inbound schema in this story). 2 Low advisories noted (regex UA-parsing on stored attacker-controlled input; `geoip_database_path` shares an existing config-path trust model), neither forcing Fail. |
| RECONCILIATION | — | reconciliation-reviewer | Done | **Pass** | docs/reviews/reconciliation/US-2.6-reconciliation.md. All 5 source ACs + FR-6/FR-7 (no-AC) have full, verified matrix coverage read against actual test assertions. No spec drift found. |
| PR | — | pr-preparer | Done | **Drafted, not opened** | docs/pr/US-2.6-pr-summary.md. All 4 gates confirmed Pass from their actual reports. `.env.example` alignment and commit hygiene both confirmed clean via `git status`. Awaiting explicit user instruction to commit/push/open. |
