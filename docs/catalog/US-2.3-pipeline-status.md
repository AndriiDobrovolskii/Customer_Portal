# Pipeline Status: US-2.3

**Active story confirmed:** Yes — user explicitly requested continuing with US-2.3 on 2026-09-01, after US-2.2 (previous active story) reached PR, was committed, and was merged as PR #5 (with a post-PR IDOR fix).
**Last updated:** 2026-09-01

| Stage | Sub-step | Skill | Status | Verdict | Notes |
|---|---|---|---|---|---|
| CLARIFICATION | — | us-clarifier | Done | **Not Ready** (resolved by user 2026-09-01) | docs/decisions/US-2.3-open-decisions.md (6 items, all resolved — recommended option accepted throughout), docs/evidence/US-2.3-clarification-report.md. A spec+review (US-2.3) already existed outside this pipeline (2026-08-22, Pass with Issues); reading the now-implemented US-2.1/US-2.2 codebase surfaced real gaps (missing `auth_audit_log.severity`, no `family_id`↔`user_sessions` link, undefined rate-limit response, unbuilt mobile-client path, undefined check order) the pre-existing spec couldn't have known about. |
| SPECIFICATION | — | story-spec-writer | Done | — | docs/specifications/US-2.3-spec.md revised 2026-09-01 to incorporate OD-1–OD-6 (new "Open Decision Resolutions" section; FR-2/FR-3/FR-6 now require Error Envelope conformance; FR-1 states cookie `Path=/v1/auth` scoping; FR-4/FR-5 split clarified against OD-5's check order; rate limit and mobile-delivery descope added to NFR/Out of Scope). |
| SPEC_REVIEW | — | story-spec-reviewer | Done | **Pass with Issues**, accepted 2026-09-01 | docs/reviews/specifications/US-2.3-spec-review.md. Full 6/6 AC coverage, no blocking contradictions (FR-2's access-token carve-out is a disclosed OD-6 substitution). 3 findings (rate-limit check ordering, email fire-and-forget, expired-vs-reused precedence) resolved same-day, spec amended. Remaining [Low] OD-anchored scope-creep note does not block. |
| DESIGN | API | openapi-designer | Done | — (2 Open Questions logged, deferred to PLANNING) | docs/designs/api/US-2.3-openapi.yaml, US-2.3-api-design.md. Single 401 (`token-invalid`) covers FR-2/FR-3/FR-4/FR-6; new 429 (`too-many-attempts`, reused slug) for OD-1's rate limit; new `refreshCookieAuth` (apiKey/cookie) security scheme, first non-bearerAuth authenticated endpoint. OQ-1: rotated-cookie attributes (HttpOnly/Secure/SameSite) assumed carried over from login, not restated by spec. OQ-2: family_id-keyed rate-limit cache doesn't exist yet, needs db-designer sizing. |
| DESIGN | DB | db-designer | Done | — | docs/designs/database/US-2.3-{db-design,entity-model}.md. `refresh_tokens` gains `consumed_at`, `last_used_at`, `ip`, `user_agent` (all nullable); `auth_audit_log` gains `severity` (nullable). FR-7's atomic consume recommended as Postgres `UPDATE...WHERE consumed_at IS NULL RETURNING` (no Valkey Lua) — no new index needed, existing `token_hash` unique index serves it. 2 items deferred: whether US-2.1's login-issuance call also populates ip/user_agent (planner), and the OD-1 rate-limit Valkey key (data-layer-builder's cache.py, out of db-designer's scope). |
| PLANNING | Impact analysis | impact-analyzer | Done | — | docs/impact-analysis/US-2.3-impact-analysis.md. No cross-module ripple. 5 additive nullable migration columns. New `TokenInvalidError` (users' own copy, per-module-duplication pattern), new `RefreshRateLimitCache`, new `EmailSender.send_refresh_reuse_alert` method, `create_refresh_token`/`create_auth_audit_log_entry` gain optional params. |
| PLANNING | Plan | planner | Done | — | docs/plans/US-2.3-implementation-plan.md. New `TokenInvalidError`, `RefreshRateLimitCache`, atomic `consume_refresh_token` (Postgres UPDATE...RETURNING), `EmailSender.send_refresh_reuse_alert` (fire-and-forget). Decided the db-designer-deferred item: login's `authenticate_user` call site now also passes ip/user_agent into `create_refresh_token`. 5 additive nullable migration columns. |
| PLANNING | Task breakdown | implementation-planner | Done | — | docs/plans/US-2.3-task-breakdown.md. T1-T8: T1(schemas)∥T2(models/repo/cache) → T3(migration) → T4(service) → T5(router/deps) → T6∥T7(tests) → T8(gate). |
| PLANNING | Plan review | plan-reviewer | Done | **Pass with Issues** (1 Low finding, fixed same-day) | docs/reviews/plans/US-2.3-plan-review.md. T4's missing ownership of pre-existing create_auth_audit_log_entry callers (severity=None) fixed directly in task-breakdown.md T4 — same gap class as US-2.2's own plan review. |
| TESTS | — | test-writer | Done | — | docs/tests/US-2.3-ac-test-matrix.md (matrix only; test code deferred to IMPLEMENTATION T6/T7, same sequencing as US-2.1/US-2.2). Covers RT-AC1-6 plus OD-1 rate-limit and OD-5/spec-review check-order edge cases. |
| IMPLEMENTATION | Schemas (T1) | schema-builder | Done | imports clean | users/schemas.py: new `RefreshResponse` (access_token, expires_in, no token_type). |
| IMPLEMENTATION | Data layer (T2) | data-layer-builder | Done | mypy strict clean | users/models.py (`RefreshToken` gains consumed_at/last_used_at/ip/user_agent; `AuthAuditLog` gains severity), users/repository.py (`consume_refresh_token` atomic UPDATE...RETURNING, `create_refresh_token`/`create_auth_audit_log_entry` gain optional params), users/cache.py (`RefreshRateLimitCache`), core/cache_keys.py (`refresh_rate_limit_key`). Found+fixed own mistake: first edit accidentally added the 4 new RefreshToken columns to UserSession instead (identical trailing text matched the wrong class) — caught by mypy, corrected before verification passed. |
| IMPLEMENTATION | Migration (T3) | migration-manager | Done, **verified** | upgrade/downgrade/upgrade proven clean via a standalone Postgres container (`customer_portal_pg`, started this session — Docker was up but no container existed) | migrations/versions/c8eeaa6b5ff6_add_refresh_rotation_columns.py. All 5 add_column ops guarded via sa.inspect(op.get_bind()), matching US-2.2's 9f9d9263bdfc exemplar exactly. Confirmed resulting schema via `\d` on both tables. |
| IMPLEMENTATION | Service (T4) | service-and-router-builder | Done | imports clean, mypy strict clean | users/service.py: new `rotate_refresh_token()` + `_handle_reuse_or_race()` helper implementing the full check order; `authenticate_user` now passes ip/user_agent into `create_refresh_token`; all 7 pre-existing `create_auth_audit_log_entry` call sites updated with explicit `severity=None`. users/exceptions.py: new `TokenInvalidError`. core/email.py: `EmailSender` gains `send_refresh_reuse_alert` (+ impl). Found+fixed 2 unanticipated gaps: (1) `UserRepository.get_by_id` didn't exist — needed for account-eligibility/reuse-alert-email lookups, added to T2's repository.py; (2) rotation must also create a new `user_sessions` row for the new access token's jti (otherwise it would never authenticate) — used the existing `create_session` method, no new repository method needed. Also self-caught a models.py edit that landed on the wrong class (UserSession instead of RefreshToken) via mypy before this task started. |
| IMPLEMENTATION | Router/Deps (T5) | service-and-router-builder | Done | routes registered, imports clean, mypy strict clean, `python -c "from app.main import app"` succeeds | users/router.py (`POST /auth/refresh`, cookie-only auth via `Cookie(alias="refresh_token")`, sets the rotated cookie with the same attributes as `/login`), users/dependencies.py (`get_user_service` wires `RefreshRateLimitCache`). |
| IMPLEMENTATION | Tests (T6) | test-writer | Done, **verified** | 63/63 unit tests pass, mypy strict clean, ruff clean | users/test_users_service.py extended: `FakeUserRepository` gains `get_by_id`, `consume_refresh_token` (+ `simulate_race_on_consume` flag for RT-AC6), `severity`/ip/user_agent/last_used_at params; `FakeEmailSender` gains `send_refresh_reuse_alert`; new `FakeRefreshRateLimitCache`. Found+fixed own test-helper bug: `RefreshToken.issued_at` is DB `server_default`, never set by the Python constructor — `_seed_rotatable_token` needed to set it explicitly (same pattern `_seed_session` already used), caught by a real `TypeError` on first run. 23 new tests covering RT-AC1-6 + OD-1 rate limit + check-order edge cases. |
| IMPLEMENTATION | Tests (T7) | test-writer | Done, **verified** | 46/46 integration tests pass on real Postgres+Valkey (testcontainers), mypy strict clean, ruff clean | users/test_users_router.py extended, 12 new tests for RT-AC1-6 + OD-1 429. Found+fixed 2 real test-authoring bugs: (1) the reuse test's immediate replay fell inside RT-AC6's own 10s grace window and was correctly treated as a race, not reuse — backdated `consumed_at` past the window to test genuine reuse; (2) `db_session.expire_all()` before re-querying also expired the `user` object loaded earlier, and a later `user.id` access under async SQLAlchemy raised `MissingGreenlet` — fixed by capturing `user_id` before expiry. Confirms FR-7/RT-AC6 atomicity for real under genuine concurrent load (asyncio.gather via `real_client`, exactly one 200/one 401, family not revoked). |
| IMPLEMENTATION | Gate (T8) | gate-enforcer | Done | **PASS** — 7/7 pre-commit hooks green, mypy strict clean (81 files), import-linter clean, 212/212 tests, 97.24% coverage (floor 85%; service.py 99%, router.py 100%), migration cycle proven in T3 | Found+fixed a real ripple: `EmailSender`'s new `send_refresh_reuse_alert` method broke `RecordingEmailSender` fakes in `email_verification` and `profile` modules' own unit tests (the protocol is shared core infra, not users-only) — added the method to both fakes. One pre-existing flaky integration test (`test_deactivate_concurrent_requests_only_one_succeeds`, account module, unrelated to this diff) confirmed via 3x isolated reruns + a clean full-suite rerun, all green. |
| VERIFICATION | — | implementation-verifier | Done | Pass | docs/verification/US-2.3-implementation-verification.md. Found+fixed one gap: missing dedicated "no cookie" security-case test (this project's own 4/5-case-per-route pattern). Added `test_refresh_no_cookie_returns_401`, full suite 213/213 reverified. |
| SECURITY_REVIEW | — | security-reviewer | Done | **Pass** | docs/reviews/security/US-2.3-security-review.md. All 6 §7 rows Pass/N/A. Dedicated IDOR-analog analysis (per US-2.2's own post-Pass finding): no equivalent gap — refresh has a single credential (the token itself), no second identity to cross-check. 3 [Low] advisories: CSRF (standing gap, not new), unknown-token brute-force out of OD-1's stated scope, OD-6's residual access-token window (already disclosed). |
| RECONCILIATION | — | reconciliation-reviewer | Done | Pass (2 matrix-accuracy gaps found+fixed same-day: RT-AC6 pointed at a never-written repository-test file; 2 stale function names from a line-length rename) | docs/reviews/reconciliation/US-2.3-reconciliation.md. 6/6 AC coverage, 26/26 referenced test functions confirmed to exist. No spec-vs-code drift. |
| PR | — | pr-preparer | Done | Draft ready | docs/pr/US-2.3-pr-summary.md. No commit-hygiene flags (no stale .gitignore this time — already fixed by US-2.2's own PR). |

## Blocking Stage (if any)

None. Story reached PR with every stage Pass. Draft ready for user review at `docs/pr/US-2.3-pr-summary.md`; nothing staged, committed, or pushed yet.
