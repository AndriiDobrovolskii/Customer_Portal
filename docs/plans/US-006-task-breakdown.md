# Task Breakdown — US-2.2 (spec ID US-006)

No `schema-builder` task: neither endpoint has a request body or a JSON response body (both are `204`), so `app/modules/users/schemas.py` is untouched. No `T0`-style unowned-infra tasks either (unlike US-2.1): this story touches no `app/core/*` file, no `.env.example`, and no `app/main.py` — everything stays inside `app/modules/users/` and one migration file.

| Task ID | Skill to Invoke | Layer (AGENTS.md §3) | Depends On | Files Touched | Verification Command |
|---|---|---|---|---|---|
| T1 | data-layer-builder | models/repository | — | `app/modules/users/models.py` (`RefreshToken.revoked_at`, index on `family_id`; `AuthAuditLog.scope`), `app/modules/users/repository.py` (new `revoke_session(jti)`, `get_refresh_token_by_hash(token_hash)`, `revoke_refresh_token_family(family_id)`; `create_auth_audit_log_entry` gains `scope` param — update its one existing call site's callers to pass `scope=None` where needed) | mypy clean; `grep -L "session.query"` on `repository.py` |
| T2 | migration-manager | migration | T1 | `migrations/versions/<hash>_add_logout_revocation_columns.py` | `alembic upgrade head && alembic downgrade -1 && alembic upgrade head` all succeed |
| T3 | service-and-router-builder (service) | service | T1, T2 | `app/modules/users/service.py` (new `logout(access_token, refresh_cookie)`, `logout_all(user_id)`; `get_authenticated_user` gains `allow_revoked: bool = False` param per plan's Architectural Changes; **update the existing login-flow calls to `create_auth_audit_log_entry` to pass `scope=None`**, per T1's signature change — this caller update belongs to T3, not T1, since the call site lives in `service.py`) | grep confirms zero `fastapi`/`starlette`/`HTTPException` imports; mypy clean on `service.py` (confirms every `create_auth_audit_log_entry` call site, old and new, passes `scope`); unit test proves an "unknown jti" (no session row) still returns `None` under `allow_revoked=True` (Open Question #2), distinct from a "revoked jti" (session row exists, `revoked_at` set), which resolves |
| T4 | service-and-router-builder (router) | router/dependencies | T3 | `app/modules/users/router.py` (`POST /auth/logout`, `POST /auth/logout-all`, both `204` no-body; conditional `Set-Cookie` clear on `/logout` only when a refresh cookie was present), `app/modules/users/dependencies.py` (new `get_current_user_allow_revoked`, used only by the `/logout` route function — `/logout-all` keeps the existing `CurrentUserDep`) | grep confirms zero `sqlalchemy`/`models`/`repository` imports in `router.py`; grep confirms `get_current_user_allow_revoked` is imported by exactly one router function (`/logout`) — the plan's highest-severity risk (leniency leaking to other routes) made mechanically checkable |
| T5 | test-writer | tests | T3 | `tests/unit/modules/users/test_users_service.py` (extended — `logout`/`logout_all` branches per plan's Testing Strategy: family revocation, lookup-miss skip, idempotent repeat, `allow_revoked` unknown-vs-revoked distinction; existing `create_auth_audit_log_entry` assertions updated for the new `scope` arg) | `pytest tests/unit/modules/users/test_users_service.py -v` all pass |
| T6 | test-writer | tests | T4 | `tests/integration/modules/users/test_users_router.py` (extended — LO-AC1–LO-AC5, plus the `/logout-all` no-leniency test and the refresh-cookie lookup-miss test) | `pytest tests/integration/modules/users/test_users_router.py -v` all pass against real PG+Valkey |
| T7 | gate-enforcer | — | T1–T6 | — | `pre-commit run --all-files`; `mypy app tests`; `lint-imports`; `pytest --cov=app --cov-report=term-missing --cov-fail-under=85` |

**Parallel-eligible:** none meaningfully — T1 is the sole entry point (no independent schema-layer task exists this time, unlike US-2.1's T1∥T2∥T2b), so the sequence is close to strictly linear: T1 → T2 → T3 → T4 → (T5 ∥ T6, once their respective dependencies land) → T7.

**No new `tests/conftest.py` fixture needed:** reuses the existing Postgres/Valkey testcontainer fixtures and the login-flow's existing "get an authenticated user + token" test helper, extended to also call `/logout` in the same flow.
